# Project State

_Read this at the start of any session doing feature work. Update it at the end of any session that completes a milestone. This file answers "where am I" — see CLAUDE.md for "what are the rules."_

## Current status

The trading core (money handling, order execution, market hours, DB layer with transactional locking) is built and tested. The dashboard at `app/(app)/dashboard/page.tsx` is feature-complete and **fully verified end to end in the browser by the user**: signup → $100,000 balance → a full buy/sell cycle (bought at ask, sold at bid, spread cost matched exactly) → both rejection paths logged correctly → position cleared on full sell → dashboard updated without a manual refresh → sign-out/sign-in → route guard blocking `/dashboard` while signed out → the layout fix confirmed good (columns fit, timestamps compact, panels balanced).

**Stock discovery is now built end to end**: `/discover` (search by symbol/name, a curated Popular list, a watchlist section once populated, and an asset-sync-staleness notice) and `/stock/[symbol]` (live bid/ask with spread, a candlestick chart of the last 90 days, position-if-held, buy/sell inline, watchlist star). See the "Done" entries below for the shape of each piece and the decisions behind it. A production `next build` was run clean after this round, confirming `/dashboard`, `/discover`, and `/stock/[symbol]` all resolve as dynamic (ƒ) routes and the new client-side chart library has no SSR issues. **Not verified: an actual authenticated click-through of `/discover` → `/stock/[symbol]` → buy/sell → watchlist star in a real browser** — no browser tool is available in this environment and no test-user credentials were available to log in via curl, so verification here is route-level (no 500s, correct auth redirects, clean production build) plus full unit/integration coverage of every pure function and DB operation, not a rendered-pixel check. Do a real click-through before calling this feature done-done.

**Charts and visualization, done immediately after, in the same "not yet click-through-verified" boat**: the stock detail chart gained volume, a crosshair readout, an avg-cost line, and buy/sell trade markers; Positions and the watchlist gained 30-day trend sparklines (batched into one Alpaca request per list, not one per symbol); the dashboard gained a portfolio allocation bar. Two real bugs were caught and fixed during this work before they shipped (both string-vs-numeric cents comparisons - see Gotchas), which is the main reason to treat "typechecks and tests pass" as necessary but not sufficient here.

**Chart improvements, the very next round on top of that**: intraday timeframes (15m/1h/1D/1W) with a real completed/live cache split (not just daily anymore), SMA/EMA/RSI as pure functions with a published-example cross-check, indicator toggles + a log-scale toggle on the chart, and the stock detail page reorganized into tabs (Chart/Position/Orders, price and spread always visible outside them). Unit tests: 222. Integration tests: 26 (both suites passing, verified by running them, not assumed). Same browser-click-through caveat as the two rounds before it - see Next.

Previously: a deployment-readiness pass (DB connection limits, secret-exposure guards, a pooler-verification route), and a cleanup of the entire route surface (root `/` redirects server-side, real metadata, a real favicon, the `/dev` preview gated to local dev only) ahead of the app going publicly reachable. Passing on GitHub Actions CI as of the last push before this round (not yet re-pushed with the discovery feature). The real Supabase project has the schema pushed and RLS enabled with zero policies (verified directly, including the two new tables added this round). Real Alpaca paper-trading credentials are wired up and confirmed working, including the market-data bars endpoint.

**The app is now live at `trading-sim-sigma.vercel.app`** - the Node-version deploy blocker noted here previously is resolved and the app deployed successfully at some point outside this log's own tracking (found out about it only when the user reported the dashboard broken - see the very next paragraph and its Gotcha). `/login` confirmed rendering (200); `/dashboard` confirmed redirecting correctly for a signed-out visitor (307), not crashing.

**Limit orders are now built end to end** - placement, cancellation, a background worker that fills or expires them, GitHub Actions scheduling, and UI on both the dashboard and stock detail page. See the Done entry below for the full shape and STATE.md's own honest bot-readiness gap assessment (delivered to the user in conversation). Route-level and integration-tested; **not yet seen rendered in a browser, and not yet exercised against a real open market** (built and tested while the real market was closed - see Next).

**Real production incident, found and fixed the same session it was introduced**: pushing this round's code broke the live `/dashboard` with a 500 - the dashboard now queries `limit_order_worker_runs` (a table that only ever existed in the local test database, per this round's own deliberate "don't touch prod schema without asking" policy) on every load. Confirmed the exact cause via a direct, read-only `information_schema`/`pg_enum` check against the real database before proposing anything - not guessed. Fixed with the user's explicit go-ahead via `scripts/apply-migration.ts` (the real Supabase database still can't take a plain `drizzle-kit push` - see the Gotcha on why), adding both the missing table and the missing `"expired"` enum value in one hand-written, transaction-wrapped migration (`drizzle/0003_add_limit_order_worker_runs.sql`). Verified fixed directly against the real database (both now present) and against the live deployment itself (`/dashboard` now 307s to `/login` instead of 500ing). **The real open-market worker exercise from the paragraph above is still outstanding** - this incident only fixed the schema gap, it didn't newly verify the fill-checking path against a live market.

**The autonomous trading bot is now built end to end**, following a two-phase design process (a researched proposal, reacted to and refined by the user, before any code) - see the Done entry below for the full shape. **The production migration (`drizzle/0004_add_bot_runs.sql`) is now applied** - verified directly against the real database (table, columns, both enums, `orders.bot_run_id`, all confirmed present via a read-only script, not assumed from the migration's own exit message). **The worker route was then manually smoke-tested against the real production database, over real HTTP, with a real bearer token** (a local dev server, not Vercel - `VERCEL_ENV` is unset locally and the route's own guard deliberately doesn't block that): a throwaway synthetic account (its own `userId`, never the real logged-in user's) started a real `selecting` bot run, `POST /api/worker/bot-runs` ran a real selection cycle against it - real `fetchDailyBarsForSymbols`/`fetchQuotes` calls, real RSI/SMA computation over the 12-symbol watchlist, 200 OK, no crash - and correctly found zero eligible candidates rather than forcing one (a real, plausible market state, not a bug: none of the 12 large-caps happened to be RSI-oversold-in-an-uptrend at that moment). Missing/wrong bearer tokens both correctly got 401. The throwaway account and its rows were deleted afterward, leaving production exactly as it was except for the intended schema change. **This confirms the plumbing, not a live trade**: the market was closed at test time (`before_open`), so the one thing not yet exercised is an actual entry firing and being followed through to a real exit - that still needs a run started while the market is open, either by hitting the worker manually again then or by the (still unscheduled - see below) GitHub Actions workflow once wired up. **The GitHub Actions schedule for the bot worker remains deliberately not wired up** - same caution as the limit-order worker's own first round: a worker that can place real (paper) trades autonomously shouldn't run unattended before being tested manually first, and unlike the limit-order round, this wasn't even asked to be deferred explicitly this time - it's the same lesson applied by default. A local-only `LIMIT_ORDER_WORKER_SECRET` now exists in `.env.local` (generated for this test, gitignored, not yet set in Vercel or GitHub Actions secrets) so this same manual test is repeatable without regenerating anything. **The honest edge expectation, recorded here before the first real bot trade closes, not reconstructed afterward**: close to a coin flip on raw price movement, and likely modestly negative once the bid-ask spread and slippage are subtracted - RSI-oversold-in-an-uptrend is one of the most already-studied, already-arbitraged retail technical patterns, competed away hardest in exactly the liquid names this bot's own spread filter pushes it toward; the exit trigger (RSI back above 50) is itself lagging, capturing less of a real reversion than it gives up on a loser; and every round trip pays the spread twice regardless. The measurement layer (`bot_runs` + the stats panel) exists specifically to test this expectation against real trades, not to assume it.

## Done

- **Scaffolded the project** — Next.js App Router, TS strict (+ `noUncheckedIndexedAccess`, `noImplicitOverride`), Tailwind, Vitest, Prettier. Decided: strict indexed-access on because the app constantly indexes into position/price arrays and empty-case bugs there are exactly the class of bug worth a compiler forcing function.
- **Built `lib/trading/`** (money, types, execute, market-hours, symbol) as pure functions, each with a matching test file. Decided: market hours use `Intl`/IANA timezone data rather than a hardcoded UTC offset, specifically so DST transitions (which differ between US and EU by up to two weeks a year) resolve correctly regardless of server locale.
- **Built `lib/db/`** — schema, transactional order placement with `SELECT ... FOR UPDATE` locking before any funds check. Decided: lock the account row, not just wrap the write in a transaction — a bare transaction doesn't stop two concurrent orders from both reading the same stale balance.
- **Built `lib/market/alpaca.ts`** — Zod-validated at the boundary, `cache: "no-store"`. Decided: convert Alpaca's float prices via `.toFixed(2)` before `toCents()`, never let a float touch arithmetic directly.
- **Wired Supabase auth** (`lib/supabase/server.ts`) and a thin Server Action (`app/actions/trade.ts`) that validates input, gets the authenticated user, and delegates to `lib/db/orders.ts`.
- **Pushed the schema to the real Supabase project and enabled RLS with no policies.** Verified directly against the live database (not just planned): all 4 tables exist, `relrowsecurity = true` on each, zero rows in `pg_policies`.
- **Set up CI** (`.github/workflows/ci.yml`) — unit job with zero environment variables (verified by stripping the env entirely and running clean), integration job against a Postgres service container. Decided: pin Node to `25.6.1` in `package.json` engines + `.nvmrc` + CI's `node-version-file`, matching the actual local dev version exactly rather than picking a different "safer" LTS, because the ask was CI parity with local, not a different tradeoff.
- **Added a `reviewer` subagent** (`.claude/agents/reviewer.md`) and a husky `pre-push` hook running `npm run verify` **and** `npm ci --dry-run`. Decided: `npm ci --dry-run` specifically, not `npm install --package-lock-only` — the latter turned out to mutate the lock file (added optional-dependency metadata) even when `npm ci` already worked, which would have made the drift check noisy and untrustworthy.
- **Approved a visual design system** (`.claude/skills/trading-ui-design/SKILL.md`) — dark, dense, data-first; tokens live in `app/globals.css`'s Tailwind v4 `@theme` block (no `tailwind.config.ts` in this project). Decided: desaturate `--color-loss` from Tailwind's stock red-500 to a custom `#dc5454` — WCAG luminance math showed green-500 was already _more_ luminant than red-500, so the "loss row reads heavier" complaint was a saturation/psychological-prominence issue, not a contrast one; darkening (red-600) was tried and rejected because it dropped contrast below the skill's own WCAG AA floor.
- **Built `PositionRow`** (`components/PositionRow.tsx`) + a preview page (`/dev/position-row`) showing profit/loss/stale states with real `formatCents` output. Decided: extracted market-value/unrealized-P&L math into `lib/trading/pnl.ts` after the reviewer subagent correctly flagged it as business logic living untested in a UI component.
- **Trimmed CLAUDE.md** from 37 to 28 lines (Stack and Structure sections removed — both fully derivable from `package.json` and the folder tree).
- **Built the real dashboard** (`app/dashboard/page.tsx`) — Summary, Positions, and Recent orders panels plus a market-status banner, wired to real data end to end. Decided: `getPortfolio` (`lib/db/portfolio.ts`) fetches cash, positions, and the last 20 orders in one round trip via a double LEFT JOIN (accounts → positions, accounts → a 20-row order subquery), deduplicating the resulting fan-out in JS, rather than raw SQL with JSON aggregation — keeps it type-checked through Drizzle's schema instead of hand-written Zod parsing of a JSON blob. Decided: a missing quote renders as `null` through `calculatePortfolio` (`lib/trading/portfolio.ts`) and `PositionRow`, never as zero — zero would claim the position is worthless, which is a stronger and false claim than "price unknown"; totals exclude the position's contribution and the caller gets an explicit `missingQuoteSymbols` list so it can mark the total as partial instead of presenting a wrong-looking complete number. Decided: `fetchQuotes` (`lib/market/alpaca.ts`) never throws for a market-data failure — a single bad symbol and a total Alpaca outage both collapse into the same `{quotes, failedSymbols}` shape, so the dashboard has one code path for "some prices missing" instead of two. Decided: `PositionRow` no longer computes P&L itself — it takes pre-computed valuation fields as props, so all money math stays in `lib/trading/`, not spread across the component and its caller.
- **Built the auth UI** — `app/(auth)/login/page.tsx`, `app/(auth)/signup/page.tsx`, `app/(auth)/actions.ts` (`login`/`signup`/`logout` Server Actions, Zod-validated email+password), a sign-out control on the dashboard, and `proxy.ts` gating `/dashboard` (redirects signed-out visitors to `/login`) and `/login`+`/signup` (redirects signed-in visitors to `/dashboard`). Decided: `signup` checks `data.session` after `supabase.auth.signUp()` rather than assuming success means logged-in — if the Supabase project has email confirmation enabled, `signUp` succeeds with no session, which is a real and different outcome ("check your email") from an error, not a bug to paper over. Decided: form error/info banners use `text-warn`, never `text-loss` — the design skill's hard rule reserves gain/loss red-vs-green for financial direction only, and a login error is a system state, not a loss. **Confirmed by the user's own click-through**: this Supabase project does not require email confirmation — signup lands directly on the dashboard.
- **Fixed the market status banner's contradictory wording** (e.g. "not open yet today ... opens Wednesday" when today already is Wednesday). `getMarketStatus` (`lib/trading/market-hours.ts`) now also returns `holidayName`, `isEarlyCloseToday`, and `nextOpenIsTomorrow`, computed once in the pure, tested function rather than re-derived in the component. `MarketStatusBanner` exports `openMessage`/`closedMessage` as plain string-building functions specifically so the wording itself has unit test coverage (`MarketStatusBanner.test.ts`), not just the open/closed boolean — this project has no component-render test setup (no jsdom/testing-library), so testable business-text lives in an exported pure function, not inline JSX.
- **Fixed money display: thousands separators, and a missing `$` on zero.** `formatCents` now groups the whole-dollar part (`100,000.00`, not `100000.00`) everywhere, since it's the one function every money display already calls — `toCents` was extended to accept its own grouped output back (`"1,234.56"` parses the same as `"1234.56"`), so the existing round-trip test needed no weakening. Separately, `Delta` (`components/Delta.tsx`) had no `$`-prefix logic in any branch, for any sign — it only looked like a "zero" bug because a brand-new account's only visible P&L value is zero. Fixed by making `showCurrency` a required prop (no default) so every call site has to decide, and extracted the text-building into `formatDeltaAmount` for the same reason as `MarketStatusBanner`'s message functions: testable without rendering.
- **Built the order ticket** (`components/OrderTicket.tsx`) — Buy/Sell toggle, symbol input (validated live against `SymbolSchema`), integer-only quantity, and a live estimated cost (ask for buy, bid for sell) fetched via a new debounced Server Action (`app/actions/quote.ts`) as the user types. Submits through the existing `placeTrade` action. Decided: `describeRejection` (the per-`RejectReason` messaging) lives in its own file, `components/orderMessages.ts`, not inside `OrderTicket.tsx` — that component imports `placeTrade`, which transitively imports `lib/db/client.ts` (throws immediately without `DATABASE_URL`), so keeping the pure message logic in a separate, DB-free file is what keeps it unit-testable in the default suite that runs with zero environment variables. Decided: cash and quote prices cross the Server → Client Component boundary as strings, reconstructed into `bigint` on the client — not passed as raw `bigint` props, since this stack's RSC bigint serialization support is untested and money crossing that boundary incorrectly is exactly the kind of bug that wouldn't show up until a large balance did. **Verified end to end by the user**: bought at ask, sold at bid, the spread cost matched exactly.
- **Fixed sell-side wording ("Estimated cost" on a sell implies money leaving the account, backwards).** `estimatedAmountLabel` (`components/orderMessages.ts`) returns "Estimated cost (ask)" for a buy, "Estimated proceeds (bid)" for a sell. Documented as a general rule in the design skill: check both sides of a toggle before assuming one label's wording covers both.
- **Removed `router.refresh()` from `OrderTicket`, kept only `placeTrade`'s `revalidatePath("/dashboard")`.** Both were present after the order ticket build; carrying two revalidation mechanisms when only one had been isolated meant one was silently dead weight. Removed the client-side call rather than the server-side one because `revalidatePath` inside a Server Action invoked via `startTransition` is the documented Next.js mechanism for this exact case - `router.refresh()` was the redundant one, not the load-bearing one.
- **Reworked the dashboard's panel layout** after Recent orders' 6 columns wrapped onto multiple lines at its old fixed 360px width. Changed the grid to `320px_3fr_2fr` (order ticket fixed - it's a form, not scaling content; Positions and Recent orders split 3:2 by actual column-content weight, not evenly, since Positions' signed `▲ +$1,234.56 (+12.3%)` P&L column is meaningfully wider than anything Recent orders shows) and widened the page container from `max-w-6xl` to `max-w-[1600px]` to give that split room to work with. Dropped `($)` from every table header (was the direct cause of the wrap) and moved `$` onto every cell instead, resolving the old "which one carries the currency" rule in the opposite direction — documented in the design skill along with why (`$150.00` vs. `−$797.00` alignment is solved by sign-then-`$`-then-digits ordering, not by hiding the `$` in a header). Replaced verbose timestamps ("Aug 5, 3:32 PM", wrapped onto three lines) with `formatOrderTimestamp` - relative for anything under 24h old, a short absolute date otherwise, unit-tested for the wrap-causing edge cases specifically. **Also implemented the mobile responsive fallback the design skill already specified but neither table actually had**: `PositionCard` and a card-list branch in `RecentOrdersPanel` render below `lg`, since a `<tr>` outside a `<table>` is invalid HTML and can't be produced by CSS alone from the same markup as the desktop table - this was a real gap, not a hypothetical one, caught while fixing the width issue. **Confirmed good by the user in the browser.**
- **Fixed the `-0.0%` display bug.** A $7.50 loss on $15,396.50 is −0.049%, which rounds to "0.0" at one decimal and, signed, reads as "−0.0%" - looks like a bug even though nothing here is an actual float negative zero. `Delta`'s `formatDeltaPercent` now shows `0.0%` unsigned only for an exact zero (no change at all), and `<0.1%` - no sign - for anything nonzero that would otherwise round to zero, since the direction glyph and the exact cents amount right next to it already say which way it moved. Also fixed: the flat (exactly zero) case previously omitted the percentage entirely rather than showing `0.0%`.
- **Pre-deployment hardening pass**, ahead of the first-ever deploy to Vercel. Capped `lib/db/client.ts`'s Postgres client to `max: 1` connection (postgres.js defaults to 10 per client; ×N concurrent Vercel instances could exhaust pgbouncer's connection budget in a way a single local Postgres never reveals) plus a 20s `idle_timeout` so a warm-but-quiet instance doesn't hold a connection open indefinitely. Added `server-only` guards to `lib/db/client.ts` and `lib/market/alpaca.ts` (the two files holding real secrets/connections) so accidentally importing either into a Client Component fails the build instead of silently bundling a secret - this required aliasing `"server-only"` to a no-op module in `vitest.config.ts`/`vitest.integration.config.ts`, since Vitest runs plain Node and never gets Next's webpack-level conditional swap for that package (`next/jest` does the same aliasing, for the same reason). Confirmed clean: no module-level state anywhere except the `db` client singleton itself (correct and intentional for serverless - reused per warm instance, not recreated per request), both Alpaca fetches already use `cache: "no-store"`, and a clean `next build` + `next start` shows only `/dashboard` and the new `/api/check-db` as dynamic (ƒ) — everything else is static, exactly as expected. Built `/api/check-db` (auth-gated, not public) - adapts `scripts/check-db.ts`'s checks to run through the app's own `db` singleton against a real deployed instance, and reports `instance.likelyWarmInvocation` (compares "now" to when this serverless instance's module first loaded) so repeated hits show whether the connection is genuinely being reused across invocations.
- **Cleaned up the whole route surface** ahead of the app going publicly reachable. `app/page.tsx` was still the unmodified create-next-app starter (Next.js logo, "Deploy Now" buttons) - it's now a Server Component with no markup of its own, just `redirect(user ? "/dashboard" : "/login")`, exactly mirroring how `app/dashboard/page.tsx` already does its own auth check. Deleted every leftover starter asset (`public/*.svg`, `app/favicon.ico`) and replaced the favicon with a hand-written `app/icon.svg` (dark background, three ascending green bars) picked up automatically by Next's file-based metadata convention. Set real `metadata` in the root layout (title, a description that explicitly says "paper-trading simulator... every dollar is virtual," and `robots: { index: false, follow: false }` - a personal project, not meant to be discovered). **Decided to keep the Geist font setup** rather than treat it as leftover: `app/globals.css` wires `--font-sans`/`--font-mono` to `--font-geist-sans`/`--font-geist-mono`, so it's load-bearing for every `font-mono`/`font-sans` utility in the entire app, not decorative scaffolding - deleting it would have been a real regression, not cleanup. Gated `/dev/position-row` (a design-reference page, not part of the app) behind `process.env.NODE_ENV !== "development" → notFound()` rather than deleting it outright - it's still useful for iterating on `PositionRow`'s visuals locally, costs nothing to keep, and this check makes it unreachable on any built output (Preview or Production both run as `NODE_ENV=production`), confirmed by hitting it on an actual local production build and getting a real 404. Also fixed stale `"($)"` column headers on that same preview page that had drifted out of sync with the currency-in-cells rule from an earlier round. Confirmed `/login`/`/signup` share the exact same root layout, font variables, and design tokens as `/dashboard` (there's only one root layout in this app; the `(auth)` route group doesn't define a competing one) - not a differently-styled island.
- **Fixed the failed Vercel deploy: Node 25.6.1 → 24.19.0.** `package.json`'s `engines.node` (`"24.x"`) and `.nvmrc` (`24.19.0`) now both name an even-numbered LTS major; `.github/workflows/ci.yml` already read from `.nvmrc` rather than a hardcoded version (confirmed, not changed) so CI and Vercel can't drift apart again. Checked every direct dependency's own `engines` field rather than assuming - the highest floor is `@supabase/supabase-js`'s `>=22.0.0`, comfortably under 24 - and grepped the codebase for recent runtime built-ins (`structuredClone`, `node:sqlite`, `Object.groupBy`, `Uint8Array` base64 methods, etc.); none appear. Confirmed empirically, not just by reasoning: downloaded the real Node 24.19.0 Windows binary into the scratchpad (no system-wide install, no touching the machine's actual Node), and ran `npm run verify`, `npm run test:integration`, and a clean `npm run build` against it directly - all pass, matching Node 25's results exactly. Added `scripts/check-node-engine.ts`, wired into `npm run verify` (and therefore the pre-push hook): parses the major version out of `engines.node` and fails loudly if it's odd, since Node's own release policy makes even-vs-odd a permanent, durable rule (every even major becomes LTS every October; odd majors never do) rather than a snapshot of today's supported list. The actual check is a pure exported function (`checkEvenLtsMajor`), covered by `scripts/check-node-engine.test.ts` (7 cases: valid range/exact/bare-number forms, an odd major, a missing field, an unparseable string) - picked up automatically by the default `npm run test` glob, no config change needed. The script itself only runs its file-reading/`process.exit` side effects when invoked directly (guarded via `process.argv[1] === fileURLToPath(import.meta.url)`), not when imported for testing.
- **Fixed an unrelated formatting issue found while doing the above**, not part of what was asked but blocking `npm run verify` regardless: `format:check` was failing across most of the repo, reproducibly under both Node 24 and Node 25 (so unrelated to the Node change itself). Cause: this machine's `core.autocrlf=true` (Git for Windows' default) puts CRLF line endings on disk for checked-out files, while `.prettierrc` had no `endOfLine` setting, so Prettier defaulted to `"lf"` and flagged every CRLF file. Fixed by setting `endOfLine: "auto"` in `.prettierrc` (Prettier now preserves whatever line ending is already in a file instead of fighting the OS/git checkout behavior) - confirmed this resolves it reliably, where trying to force LF via a new `.gitattributes` alone did not (`git add --renormalize .` found nothing to change since git's own LF-normalized storage was already correct; a forced `git checkout-index -f -a` still left CRLF on disk, root cause not fully pinned down beyond "something about this Windows checkout path re-applies autocrlf even with a matching `eol=lf` attribute present"). Kept the `.gitattributes` anyway as a second layer for future clones, but `.prettierrc`'s `endOfLine: "auto"` is the fix that's actually verified to work.

- **Researched Alpaca's actual free-tier data availability empirically before building anything** (search/browse/discovery had been entirely missing - the app only supported trading a symbol you already knew). Verified via real API calls against this project's own credentials, not docs alone: 14,195 real active US-equity assets on `/v2/assets` (trading API, not market-data API), zero sector/industry field on any of them ever, the SIP-feed free-tier restriction is specifically "no querying roughly the last 15 minutes" (confirmed via two precisely-timed real calls), and `most-actives`/movers endpoints work but return low-quality, noisy results - rejected as a "Popular" data source for that reason. Proposal accepted as written; implementation followed a fixed 7-step order (assets sync → search → nav → `/discover` → watchlist → `/stock/[symbol]` → chart last) specifically so every piece before the chart stayed independently usable even if chart integration turned out fiddly.
- **Split CLAUDE.md's caching rule into three cases** (live quotes / completed historical bars / today's still-forming bar) instead of one blanket "never cache market data" line - the third case (today's bar comes back from the same bars endpoint as history but is still live, not historical) is the one the old wording didn't distinguish and would have been easy to get wrong.
- **Built the assets table + idempotent sync script** (`lib/db/assets.ts`, `scripts/sync-assets.ts`) so search has something fast to query against (Alpaca has no search endpoint, only a bulk list). `syncAssets()` upserts every fetched asset with a single per-run `asOf` timestamp, then flips anything previously `tradable = true` but not touched by the run to `false` (never deleted - a position or watchlist entry can still reference a delisted symbol). An append-only `asset_sync_runs` log (not a single mutable "last synced at" column) so staleness detection can tell "no successful sync in N days" apart from "syncs have been failing every day for N days." A `MIN_EXPECTED_ASSET_COUNT` guard rejects a suspiciously small response (more likely a bad fetch than a real market-wide delisting event) before it can mark thousands of real assets stale. **Run twice against the real database, confirmed idempotent**: 14,194 tradable assets both times, 0 marked stale on the second run.
- **Built DB-backed search** (`searchAssets` in `lib/db/assets.ts`) rather than an in-memory cache, and benchmarked it directly against the real ~14,200-row table before deciding whether an index was needed: a plain `ILIKE` seq scan (symbol OR name, tradable-only) ran in 30-60ms across a range of real queries - comfortably inside typing-speed budgets - so no trigram index was added. Ranks an exact symbol match first, then a symbol-prefix match, then everything else alphabetically, via a `CASE` expression in the `ORDER BY`.
- **Added the search-escape-hatch and delisted-symbol decisions the user explicitly asked to be made, not deferred**: (1) if a search term is ticker-shaped (passes `SymbolSchema`) but matches nothing locally, `StockSearch` offers a direct link to `/stock/[symbol]` that bypasses the local table entirely - resolved against a live Alpaca quote instead of guessing. (2) The stock detail page independently signals both directions of asset-list drift: a quiet note when a symbol isn't in the local table yet but Alpaca has a live price for it ("the asset sync may be overdue"), and a `text-warn` note when the local table already knows a symbol is `tradable: false`. (3) If a symbol has no live quote at all (delisted, or Alpaca-invalid), `StockQuoteCard` shows an explicit warning and `OrderTicket`'s own independent client-side quote check disables Buy/Sell with "No price available for this symbol right now" - the order can't reach `placeMarketOrder` with a dead symbol in the first place.
- **Built the nav header** (`components/Nav.tsx`) and moved `dashboard`/`discover`/`stock/[symbol]` under a new `app/(app)/layout.tsx` route group, which now does the auth-redirect-to-`/login` check once for all three instead of each page repeating it (URLs are unchanged - a route group adds no path segment). `Nav` is a Client Component using `usePathname()` to highlight the active link, since a shared Server Component layout has no per-page "which link is active" signal without extra plumbing; `/stock/*` highlights "Discover" (that's the only place a stock page is ever reached from).
- **Built `/discover`** - search box, a watchlist section shown only once populated (an empty watchlist panel here would say nothing Popular doesn't), then a curated Popular list (12 large-cap symbols, hand-picked since Alpaca's movers/actives endpoints were already rejected as a data source - each symbol/name verified against the real synced table before use). Both lists show live bid/ask, never a single price, fetched via the existing `fetchQuotes` batch call. The asset-sync staleness notice (`AssetSyncNotice`/`describeAssetSyncStatus`) is always rendered, never hidden even when fresh - same "staleness the user can't see isn't staleness that gets fixed" philosophy as `MarketStatusBanner` - escalating from a quiet one-liner to a `text-warn` banner past 7 days since the last successful sync, or if there's never been one.
- **Built the watchlist** - a `watchlist_items` table (unique per account+symbol), `toggleWatchlistItem` (delete-then-insert, not read-check-then-write, so the delete's row count IS the "is this watched" check with no separate read a concurrent toggle could invalidate first), and `WatchlistStar` (optimistic client toggle, reverts on failure). Star color is `--color-accent`, deliberately not gain/loss/warn - this toggle carries no financial-direction or data-freshness meaning, and the design skill reserves those colors strictly for that.
- **Built `/stock/[symbol]`** - header (name/exchange/watchlist star), live bid/ask with spread (`StockQuoteCard`, mirroring the "never a single price" hard rule even outside order execution), position-if-held (`StockPositionSummary`, reusing `lib/trading/pnl.ts`'s `valuePosition` - no new money math), and `OrderTicket` extended with an optional `fixedSymbol` prop rather than a duplicate ticket component, since the debounced quote-fetch and rejection-messaging logic already existed and a second copy would only be able to drift from it. **Caught and fixed a real bug while wiring this in**: `OrderTicket`'s success handler unconditionally cleared `symbolInput` to `""`, which would have bricked a `fixedSymbol` ticket after its first trade (the input can't be retyped on this page) until a manual refresh - fixed by only clearing it when the symbol was free-text input to begin with.
- **Added `fetchDailyBars`** (`lib/market/alpaca.ts`) and integrated `lightweight-charts` (v5, candlestick series) as the very last piece, per the agreed build order. Verified two Alpaca-specific facts empirically before writing the function, not from docs: `feed=iex` is _required_ on this endpoint specifically (the default feed is SIP, which 403s - "subscription does not permit querying recent SIP data" - on any range touching recent days on this free-tier account; quotes don't need this because their default feed doesn't hit the same restriction), and `bars` comes back as `null`, not `[]`, for a symbol with no data (confirmed against a nonexistent symbol, which still returns 200). Deliberately always fetched live (`cache: "no-store"`) rather than splitting completed-vs-forming days into a cached/uncached pair as CLAUDE.md's caching rule would technically allow: today's still-forming bar rides along with every closed day in the same response (confirmed empirically - the most recent bar on a trading day has a fraction of a normal day's volume), and this endpoint isn't a hot path the way quotes are, so the split would add real complexity for no demonstrated need. The chart itself reads its colors from the real CSS custom properties in `globals.css` at runtime (`getComputedStyle`) rather than hardcoding a second copy of the same hex values, since a canvas library needing literal color strings is a legitimate exception to "semantic tokens only," but duplicating the palette as a parallel literal copy is not.
- **Hit and worked around a confirmed, unpatched upstream `drizzle-kit push` bug** (`drizzle-team/drizzle-orm#4496` - a foreign key misclassified as a check constraint) while adding both new tables this round. Reproduces against the real Supabase database, does not reproduce against local test Postgres with the identical schema. Worked around by hand-writing migration SQL (conventions verified against live `information_schema.columns`/`pg_constraint` output from the closest existing analog table) and applying it via a new `scripts/apply-migration.ts` (a raw `postgres()` connection in one transaction, bypassing `lib/db/client.ts`'s `server-only` guard entirely). Confirmed correct afterward by querying columns/indexes/FK constraints/RLS directly against the real database each time, not assumed from the migration file alone.
- **Found and fixed a real, previously-undetected regression while building the sync script**: `npm run quote` was silently broken by an earlier session's own `server-only` hardening (`Error: This module cannot be imported from a Client Component module` when run via plain `tsx`, because Node never sets the `react-server` export condition Next's build sets automatically). Fixed via `cross-env NODE_OPTIONS=--conditions=react-server` prefixed onto both the `quote` and `sync-assets` npm scripts - the same resolution condition Next's own build uses, not a workaround around it.
- **Added `toExchangeDateKey`** (`lib/trading/market-hours.ts`) - maps a UTC instant onto its Eastern trading-day date, reusing the file's existing DST-aware `Intl` machinery. Built specifically so stock-chart trade markers place correctly: a naive UTC-date slice on an order's `filledAt` happens to agree for any regular-hours fill (9:30am-4pm ET never crosses a UTC midnight boundary), but that's a coincidence of this app never having extended-hours trading, not a guarantee - tested explicitly across the EST/EDT boundary and the just-after-midnight-UTC case where a naive slice would be wrong.
- **Upgraded the stock detail chart**: a volume histogram (overlaid on the same pane via a dedicated price scale confined to the bottom ~25% via `scaleMargins` - lightweight-charts' long-established pattern, chosen over its newer separate-pane API since there's no browser here to visually confirm the newer API's behavior), a crosshair readout (date/OHLC/volume, updated via direct ref writes so hover doesn't re-render React on every mouse-move), a dashed avg-cost price line in `--color-accent` when a position is held, and buy/sell trade markers built from a new `getFilledOrdersForSymbol` (`lib/db/orders.ts`) read. Pure formatting/mapping helpers live in `components/stockChartFormat.ts`, separate from `StockChart.tsx` itself (which calls `createChart` and touches the DOM), so they stay importable by the default unit suite that runs under Vitest's node environment with no DOM.
- **Caught two real bugs during the chart work, before they shipped**: (1) volume-bar coloring compared cents as strings (`"10050" >= "9999"` is `false` lexicographically even though 10050 is numerically larger) - fixed by comparing as `BigInt` (`isUpBar`). (2) the crosshair handler assumed lightweight-charts hands back the same `"YYYY-MM-DD"` string passed in for a bar's time, which isn't guaranteed - it may normalize to a `BusinessDay` object internally. Fixed using the library's own exported `isBusinessDay` type guard (`timeToDateKey`) rather than assuming its internal representation. Both are exactly the class of thing "it typechecks and the tests pass" doesn't catch on its own - caught by re-reading the diff with the specific question "is this comparison doing what it looks like it's doing."
- **Added 30-day trend sparklines** to Positions (`app/(app)/dashboard/page.tsx`) and the watchlist (`app/(app)/discover/page.tsx`), not the curated Popular list (not something a user is tracking). The cost question the user asked to have answered explicitly: `fetchDailyBarsForSymbols` (`lib/market/alpaca.ts`) batches an entire symbol list into one request via Alpaca's multi-symbol bars endpoint, the same batching this file already does for quotes - a 20-symbol watchlist costs one network call, not 20. Confirmed empirically that this endpoint's `limit` caps the TOTAL bars across every symbol in the response, not per symbol (a too-small limit silently returned only the first symbol and dropped the rest, with no error) - sized generously to avoid that. No caching layer was added on top of the batching (considered and rejected, same reasoning as `fetchDailyBars`: batching already solves the N-calls problem, and splitting completed-vs-forming days to cache just the closed ones would add real complexity nothing here yet needs). `Sparkline` (`components/Sparkline.tsx`) is a plain server-renderable SVG polyline, not a `lightweight-charts` instance - a 20-row list would mean 20 full chart instances for a glance-sized shape. Colors come from `currentColor` plus a `text-gain`/`text-loss`/`text-muted` class, not a `stroke-*` utility, since only `bg-*`/`text-*`/`border-*`/`ring-*` are confirmed to be generated from this project's theme tokens.
- **Added a portfolio allocation breakdown to the dashboard** (`PortfolioAllocation`, backed by the pure `calculateAllocation` in `lib/trading/allocation.ts`). Chose a horizontal stacked bar plus a labeled legend over a donut/pie, per the explicit ask to recommend a form rather than default to one: at this app's scale (a handful of positions), a stacked bar reads proportions more accurately than angle/area comparisons in a pie, and fits the app's existing dense, table-driven visual language better than an isolated decorative widget would. Cash is always its own slice (placement doesn't depend on its size - it isn't "an investment" the way a held symbol is), a position with no live quote is excluded rather than shown as zero, and position slices use a decreasing-opacity scale of the existing `--color-accent` token rather than a new categorical palette - this project's token set has no qualitative/categorical colors, and inventing 5+ new arbitrary hues for one chart's legend would be a bigger design-system decision than this task called for.
- **Investigated what a real equity curve would take, without building it** (explicitly deferred - see Open questions/deferred). The transactions ledger has no historical portfolio-value record, only cash deltas, so a curve needs either (a) a forward-only daily snapshot table recorded from now on, or (b) a one-time retroactive backfill that replays the existing order history against Alpaca's historical daily closes to reconstruct past equity. Both are real options here, not just (a) - see the write-up below for the tradeoff.
- **Added intraday timeframes (15m/1h/1D/1W) to the stock chart**, verified empirically before building anything: 15Min/1Hour bars both need `feed=iex` (the default SIP feed 403s on any range touching recent minutes/hours, the same restriction and ~15-minute boundary already found on daily bars), and IEX intraday data goes back at least 2 years on this account - no retention limit to design around. `fetchBars` (`lib/market/alpaca.ts`, replacing `fetchDailyBars`) implements CLAUDE.md's three-way caching rule for real, not just for daily: the request is split in two at `currentBarStart` (exported, tested) so the completed half's URL - and therefore its Next.js Data Cache key - stays identical for every call made during the same still-forming bar period, with `cache: "force-cache"` explicitly opting back into caching despite the page being `force-dynamic`. An earlier version tried to skip the live half entirely while the market is closed for better off-hours cache efficiency; reverted after it broke a real case (a weekday evening's own, by-then-complete daily bar would have been silently dropped) - documented the off-hours cache-churn tradeoff honestly instead of chasing the optimization further. `startOfExchangeDay`/`startOfExchangeWeek` (`lib/trading/market-hours.ts`) provide the DST-aware boundaries this needs; the weekly one computes via whole-calendar-day subtraction in an abstract UTC calendar, converting to a real instant once at the end, rather than subtracting milliseconds from an already-DST-resolved instant.
- **Made invalid timeframe/range combinations impossible, not just slow** (`lib/market/chart-timeframes.ts`): a fixed compatibility matrix (finer timeframes drop out as the range grows - 15Min past 1M, 1Hour past 3M) plus a deterministic fallback (`nearestValidTimeframe`) so switching ranges never leaves the chart with no valid timeframe selected. The chart is now genuinely interactive - a Server Action (`app/actions/bars.ts`) re-fetches on timeframe/range change, reusing `fetchBars`' own cache split rather than adding a second layer on top. Trade markers and the crosshair readout now match against the actual bar containing a fill's timestamp (`findBarIndexAtOrBefore`) instead of exact date-string equality, generalizing correctly to any timeframe instead of being daily-only.
- **Added SMA, EMA, and RSI as pure functions** (`lib/trading/indicators.ts` - no chart library imports, no fetching, no React). Every index without enough preceding data is `null`, explicitly, never 0. EMA's first value is the SMA of the first `period` values, not the first value alone. RSI uses Wilder's smoothing (a running weighted carry-forward), not a plain moving average of gains/losses. **Cross-checked against an independent source, not just internal test consistency**: a price series was reconstructed to match the aggregate gain/loss totals of a published worked RSI example (T3 Live's RSI tutorial), reproducing its reported RSI values (77.76 → this implementation's 77.78, explained entirely by the example's own intermediate rounding; 73.38 → matches almost exactly) - see the indicators.test.ts comment for the reconstruction.
- **Added indicator toggles and a log-scale toggle to the chart**: a restrained toggle list (label + adjustable period, not a full indicator dialog) for SMA/EMA (overlaid on the price panel in two new dedicated tokens, `--color-chart-line-1`/`-2`, added deliberately to `globals.css` and documented in the design skill - a price chart overlays a small, fixed number of named lines, a bounded need unlike `PortfolioAllocation`'s unbounded per-position slice count) and RSI (its own 0-100 price scale, pinned via each series' `autoscaleInfoProvider` so a flat run near 50 doesn't auto-zoom). Points before an indicator has enough data are omitted from its line entirely, not plotted as 0. Log scale applies only to the main price scale, never volume or RSI (both already bounded/normalized).
- **Reorganized the stock detail page into tabs** (`StockDetailTabs`): Chart, Position (holding + the buy/sell ticket together), Orders (this symbol's own fill history, new content via `StockOrderHistory`, not the dashboard's all-symbols `RecentOrdersPanel`). Price and spread stay outside the tabs, always visible, per the explicit instruction not to hide them. The tab bar uses an underline style, deliberately different from the chart's own segmented "chip" controls (Interval/Range/Indicators) - page-level navigation vs. in-panel controls, not an inconsistency.
- **Fixed four bugs the user caught from a real screenshot of the chart** - the first time this feature was actually seen rendered, not just route-verified, and it immediately found real problems the browser-click-through gap in "Next" had been flagging as a risk. (1) **Duplicate candle at the boundary between historical and live data** - confirmed empirically that Alpaca's bars `start`/`end` are both inclusive, so the completed-range query (`end=formingBarStart`) and the forming-range query (`start=formingBarStart`) both returned the exact bar sitting on that shared instant whenever one existed there. Fixed in `fetchBars` (`lib/market/alpaca.ts`) by subtracting 1ms from the completed query's `end`, plus a `dedupeBarsByTimestamp` safety net (keeps the later/forming version on any remaining collision) - both covered by new tests in `alpaca.test.ts`. (2) **SMA/EMA never drew and RSI's panel never appeared** - not a wiring bug: `smaEnabled`/`emaEnabled`/`rsiEnabled` were already correctly in the chart-rebuild effect's dependency array. The actual problem was pure discoverability - the toggle button had no border/background in its "off" state, so it read as inert text next to the period `<input>`, which looked like "the" control. Fixed with an always-visible border in both states plus a filled/empty dot glyph (`●`/`○`) on `IndicatorToggle` (`StockChart.tsx`). (3) **Volume's "606.89K" last-value label bled into the price panel's 240-260 band** - the hidden-overlay-price-scale technique (a second named price scale confined by `scaleMargins` + `visible: false`) doesn't actually confine a series' `lastValueVisible` badge the same way it confines the bars themselves; this is a genuine limitation of that technique, not a tuning problem. Fixed by switching volume and RSI to lightweight-charts' real multi-pane API (`chart.addSeries(type, options, paneIndex)` + `chart.panes()[n].setHeight()`) - previously avoided only because it couldn't be visually confirmed without a browser, now adopted because the alternative is now confirmed broken by real evidence. (4) **Closed the "inert end-to-end" gap the user explicitly asked to have checked** - extracted `buildLineSeriesData` (`stockChartFormat.ts`) as a pure, tested function that turns a bars+values pair into the exact points fed to `setData`, so SMA(20) over a ~22-bar 1M/1D window is tested to produce only its few valid trailing points (never a plotted zero, never a flat line) through the same code path the chart actually calls, not a parallel untested copy. Unit tests: 228. Integration tests: 26 (both re-run and passing after this round). Full verdict per issue delivered to the user in conversation (bug / bug / bug / discoverability, not a wiring bug).
- **Fixed three more issues from a second real screenshot, plus a real money-safety bug found while checking for the same family.** (1) **The lightweight-charts TradingView logo** - disabled via `layout.attributionLogo: false` (confirmed against the library's own license docs and Apache-2.0 NOTICE file: the free tier permits this, but the license's required link to tradingview.com must then be preserved some other way, not dropped). Replaced with a small static, non-canvas text credit below the chart - kept as a real link (not bare text), since the license's own docs specify a link, not just attribution text. (2) **The avg-cost dashed line's on-canvas axis label ("Avg cost 308.08") collided with a recent trade marker's "S 20" label** when the trade happened near both the current price and the right/recent edge of the visible range - a real, not hypothetical, overlap since the axis label's position is pinned to price only and a marker's position is pinned to time+price, so nothing prevents them sharing screen space. Fixed by turning the price line's `axisLabelVisible` off (keeping the dashed line itself as visual context) and moving the actual "Avg cost $X" text into the chart's existing fixed top-left HTML overlay (same one used for the OHLC/indicator readout) - plain HTML outside the canvas entirely, so it can never collide with a marker regardless of when or at what price a trade happened. (3) **A real, confirmed bug: Bid $295.94 / Ask $0.00 / Spread −$295.94 rendered while the market was closed** - not a rendering issue, a data-validity gap. Alpaca reports 0 for a side with no active quotation (confirmed the real cause, not assumed) rather than omitting the field, and neither `fetchQuote` nor `fetchQuotes` (`lib/market/alpaca.ts`) validated that both sides were actually positive before treating the response as a real quote. Fixed at the source: both now treat a zero-priced bid or ask as no quote at all (new `NoTwoSidedQuoteError` for the single-symbol path; silently excluded from the map, falling into `failedSymbols`, for the multi-symbol path) - this is the one shared root cause behind every affected render site, not several independent bugs, confirmed by checking each one: `StockQuoteCard`, `WatchlistRow`/`WatchlistCard`, `PopularStockRow`/`PopularStockCard`, and `OrderTicket`'s estimate all already had a "null bid/ask means no quote" branch from earlier rounds, so excluding an invalid quote at the fetch boundary made all of them correct for free, with zero changes to any of those files except a defense-in-depth check added to `StockQuoteCard` itself (new `lib/trading/quote.ts`: `isValidTwoSidedQuote`/`calculateSpreadCents`, both tested). **The more serious version of the same bug, found by checking "anything else in the same family" rather than assumed**: `placeMarketOrder` (`lib/db/orders.ts`) calls the same `fetchQuote` to price an actual fill - before this fix, a zero-priced ask/bid could have filled a real order at $0.00 the moment it happened while the market was technically open (thin liquidity, not just closed-market hours; the existing `market_closed` rejection only guards the closed-market case, not this one). Fixed by catching the new `NoTwoSidedQuoteError` specifically and turning it into an ordinary `{ok: false, reason: "no_quote"}` rejection (a new `RejectReason`, with its own message in `orderMessages.ts`) - a real rejected-order row still gets written, per CLAUDE.md's "order rejection is a normal outcome, not an exception" rule, not an unhandled throw. Unit tests: 241. Integration tests: 28.
- **Built limit orders end to end**: placement, cancellation, a background worker, scheduling, and UI - the largest single round in this log. Architecture decision, stated up front and followed throughout: placement and cancellation are Route Handlers (`app/api/orders/limit/route.ts`, `app/api/orders/[id]/cancel/route.ts`), not Server Actions, specifically so a future API-key-authenticated bot can call them directly - a Server Action has no meaning to a caller outside this app's own React tree. `lib/auth/session.ts`'s `getAuthenticatedUserId` is the one auth check every Route Handler uses (a Supabase session cookie today, structured so an API-key branch can slot in as an alternative later without touching any route). No Server Action was added for this feature at all - `OrderTicket` (a Client Component already) calls the Route Handlers directly via `fetch`, which is simpler than inventing a Server-Action wrapper the architecture note explicitly said not to build backwards.
  - **Schema**: added `"expired"` to `order_status` (a day-order limit that never filled by close - distinct from `cancelled`, a user action) and a new `limit_order_worker_runs` table, same append-only run-log shape as `asset_sync_runs` (`started_at`/`finished_at`/`status`/counts/`error_message`), so "never ran" and "ran but failed repeatedly" stay distinguishable. Pushed to the local test database; **not yet pushed to the real Supabase project** - deliberately not done without asking, since that's a production schema change, unlike everything else built and verified against the disposable local test DB this round.
  - **Pure logic** (`lib/trading/limit-fill.ts`, `lib/trading/limit-reservation.ts`, both tested with boundary cases): `shouldFillLimitOrder` is the one-line price condition (a buy fills when ask <= limit, a sell when bid >= limit, both inclusive) - deliberately separate from everything about funds/shares/market hours, which stay `executeMarketOrder`'s job. `canPlaceLimitOrder` is the placement-time solvency check: cash/shares available minus what every other pending order of the same side has already claimed (a buy reserves at its own limit price - the worst case it could still cost; a sell reserves shares, scoped to its own symbol, since shares aren't fungible across symbols the way cash is) - no new column, exactly the "derived reservation" design named up front. Reuses `RejectReason` (`insufficient_funds`/`insufficient_shares`/`invalid_quantity`) rather than inventing parallel reasons, plus one genuinely new one, `invalid_limit_price`.
  - **Placement** (`placeLimitOrder`, `lib/db/orders.ts`) locks the account row (same primitive `placeMarketOrder` uses), reads every other pending limit order fresh from inside that same lock, calls `canPlaceLimitOrder`, and inserts the order as `pending` or `rejected` - never touches Alpaca at all, unlike a market order, since a limit order's whole point is to wait.
  - **The worker** (`runLimitOrderWorker`, `lib/db/limit-order-worker.ts`, invoked via `POST /api/worker/limit-orders`) is a pure function of "what's true right now," not of scheduling history - built explicitly to be safe whether two invocations land a minute apart or a day apart. If the market is closed, it sweeps every pending limit order to `expired` unconditionally (not just ones placed today - see the Gotcha below on why, and the real tradeoff that follows from it). If open, it takes a bounded batch (100, oldest first), batches one `fetchQuotes` call for every distinct symbol in it (never one Alpaca call per order), and for each candidate: locks the order row (the same lock `cancelOrder` takes - whichever transaction gets there first wins, no separate "processing" status needed), re-checks `status === "pending"` after the lock, checks `shouldFillLimitOrder`, and if true, locks the account row and re-runs the fill through `executeMarketOrder` - the exact same path a market order uses, so a limit order fills at the current ask/bid (price improvement, never worse than its own limit) and gets funds/shares re-validated for real, not trusting the placement-time check. A fill-time `insufficient_funds`/`insufficient_shares` (which the reservation math is supposed to make structurally impossible) is treated as a genuine rejection; a fill-time `stale_quote` (a realistic timing artifact in a large batch) just leaves the order pending to retry next run. `applyFillEffects`/`recordFillTransaction` were extracted out of `placeMarketOrder` so both it and the worker share the exact same cash/position/ledger-mutation code, not two copies.
  - **Cancellation** (`cancelOrder`) locks the order row and reports one of four real outcomes - `ok: true`, `not_found` (also covers "belongs to someone else," deliberately indistinguishable from not existing), `already_filled` (the worker won the race), `not_cancellable` (already finalized some other way) - never a bare error, matching `OrderResult`'s own discriminated-union discipline.
  - **Observability** (`GET /api/worker/status`, session-authenticated like `/api/check-db`, not the worker's bearer secret) reports the last run's age, status, and counts, and flags `abnormal` when it's stale for how long the market's been open/closed (`lib/trading/worker-staleness.ts`, shared with the dashboard/stock page's own staleness banner so both agree on the same thresholds), when the last run failed, or when a run's status is stuck at `"running"` long past when it should have finished.
  - **Scheduling** (`.github/workflows/limit-order-worker.yml`, every 5 minutes via `workflow_dispatch`-enabled `cron`) calls the deployed worker route with a bearer secret (`LIMIT_ORDER_WORKER_SECRET`, new env var) read from repo secrets (`WORKER_URL` + the secret). A second workflow (`keepalive.yml`) makes a trivial monthly commit specifically because GitHub auto-disables every scheduled workflow in a repo after 60 days with no commits - not no runs - which the worker's own every-5-minutes schedule does nothing to prevent on its own. The worker route itself also checks `VERCEL_ENV` and refuses to run outside `production`, since a preview deployment would otherwise point at the same real database and could race the real scheduled worker.
  - **UI**: `OrderTicket` gained a Market/Limit toggle - a limit order needs no live quote or open market to be accepted (unlike a market order), so its own disabled-reason logic and estimate ("Max cost (limit)"/"Min proceeds (limit)", a firm bound, not an estimate) are genuinely separate paths, not the market order's logic reused with a flag. A new `PendingOrdersPanel` (dashboard, and the stock page's Position tab filtered to that symbol) lists resting orders with a Cancel button calling the Route Handler directly, and shows a visible warning when the worker's last run is stale enough that a pending order might not actually be watched - never letting a user believe otherwise.
  - **Tests, with the `max: 1` lesson applied deliberately**: every concurrency test asserts `poolMax >= 2` first, with a message explaining exactly why a lower value would make it pass trivially - not just copied from the existing market-order test, but written fresh for each new race. Two real races got dedicated, deterministic proofs: cancellation losing to an in-progress fill (a held raw-connection lock, timed, proving `cancelOrder` genuinely blocks and then correctly reports `already_filled`) and, more importantly, **double-claim prevention** - two full `runLimitOrderWorker()` invocations racing for the exact same order (via the same delayed-mock-resolves-in-lockstep technique the market-order test already established), asserting exactly one of them fills it, never both, never neither. All six real Alpaca quote snapshots from the earlier spread-bug round, plus new ones, continue to live as fixtures rather than only being described in prose. Unit tests: 283. Integration tests: 49 (28 existing + 21 new).
  - **Verified beyond the test suite**: a clean `next build` shows all four new routes as dynamic (ƒ); a running server against the local test DB confirmed every route's auth gate (401 unauthenticated, 200 with the correct worker secret) and a real end-to-end lifecycle (place → reservation-reject an oversized order → reject a zero limit price → cancel → cancel-again correctly `not_cancellable` → cancel-unknown correctly `not_found` → a real worker run) via a throwaway script exercising the DB layer directly against real Postgres, not mocked.
- **Closed the single biggest gap named in this session's own "how far from bot-ready" assessment: there was no read side.** Added `GET /api/orders` (optional `?status=` filter over all five statuses, via a new `getOrdersForAccount` - a third, deliberately distinct order-reading query alongside `getPortfolio`'s dashboard-shaped `recentOrders` and the reservation-math-shaped `getPendingLimitOrdersForAccount`, since a read API needs the complete shape - `type`, `limitPriceCents` included - that neither existing query carries) and `GET /api/portfolio` (reuses `getPortfolio` + the exact same `calculatePortfolio` the dashboard itself calls, so the API and the dashboard agree on valuation by construction, not by staying in sync manually). Same `getAuthenticatedUserId` session-cookie auth as the write routes. Both return cents as numeric strings, not bigint - not just this app's usual convention here but a hard requirement, since `NextResponse.json`'s underlying `JSON.stringify` throws outright on a raw bigint. Verified end to end against the real test database (auth gate 401 unauthenticated; a full walkthrough - place a limit buy, confirm `pending` not `filled`, over-commit a second order against the first's reservation and confirm `insufficient_funds`, cancel the first and confirm it reads back as `cancelled` via the exact query `GET /api/orders` uses, run the worker and confirm the observability query's null → populated transition) via a throwaway script, since no real browser session can be produced in this environment - see the note to the user about what that leaves for an actual browser click-through. Unit tests: 283 (unchanged - both new routes are thin wrappers with no route-level test convention anywhere else in this app either). Integration tests: 52 (49 + 3 new for `getOrdersForAccount`).
  - **Everything else from that gap assessment was deliberately NOT built, and is recorded here as the explicit list for whenever the bot milestone actually starts** (not built ahead of need, per instruction): API-key authentication itself (the one thing that actually lets a bot use any of this - `getAuthenticatedUserId` has a documented slot for it, nothing more); idempotency keys for `POST /api/orders/limit` (a bot's retry after a timed-out request could otherwise place a duplicate order); rate limiting on the write routes (nothing throttles an authorized script the way human click-speed naturally throttles a browser); a webhook/push notification for fills (a bot today can only poll `GET /api/orders`, there's no way to be told); a versioned, machine-readable contract (OpenAPI/JSON Schema - today the contract is "read the route source"); and bulk/batch place-or-cancel endpoints (a bot managing many symbols needs one HTTP round trip per order today). None of these are half-built - none of them exist at all yet.
- **Found the non-zero sibling of the same bug from a follow-up screenshot (NVDA: Bid $210.44 / Ask $232.74, a $22.30/10.1% spread - implausible for a stock that normally trades pennies-wide) and confirmed empirically, not assumed, that it's the same root cause in a different numeric shape, not a second bug.** Pulled Alpaca's raw latest-quote response directly (bypassing this app's code) for six liquid symbols at once: all six were a single tick, one shared timestamp, one exchange code (`bx`/`ax` both `"V"` - IEX) - never two quotes from different points in time stitched together. NVDA (10.1%) and TSLA (9.8%) showed the wide-but-nonzero shape; AAPL/MSFT/AMZN/SPY showed the fully-zeroed-ask shape the previous fix already catches. Root cause either way: this account's free data plan only sees IEX, and IEX's own book is genuinely thin right at the 4pm ET close, since most of a stock's real closing volume clears through the primary listing exchange's closing auction, not IEX. **Decided to flag, not reject or block trading on it** - unlike a zero-priced side (unambiguously invalid), a wide spread is still a real quote, and hiding it behind the existing "no live price" state would suppress genuine (if venue-thin) data from an app whose purpose is showing real prices; a numeric threshold is also inherently a heuristic that could misfire on a legitimately volatile small-cap. Added `isSpreadImplausiblyWide`/`describeWideSpreadWarning` to `lib/trading/quote.ts` (300bps/3% threshold - comfortably above any liquid stock's normal continuous spread, comfortably below the ~1000bps observed artifact) and surfaced the warning as a visible caveat, not a blocker, in both `StockQuoteCard` and `OrderTicket`'s estimate. Order execution itself was deliberately left unchanged: `market_closed` already blocks a real fill in the closing-print scenario specifically, and gating actual trades on a spread heuristic during genuine market hours is a bigger behavior change than what this was - flagged as a known limitation below rather than decided silently. Unit tests: 246.
- **Fixed two real usability gaps in the order ticket, both in `OrderTicket.tsx`, and along the way fixed a real, pre-existing search-ranking bug the task's own acceptance criterion exposed.**
  - **Symbol search**: replaced the plain text symbol input with `SymbolAutocomplete` (new component) - reuses `/discover`'s exact DB-backed search (`searchStocks` -> `searchAssets`' ILIKE query), not a second search implementation, but doesn't reuse `StockSearch.tsx` itself, since selecting a result there navigates to `/stock/[symbol]` while selecting one here must fill the field and keep the user in the ticket - different interaction contracts on the same data source. Fully keyboard-navigable (`↑`/`↓` to move the highlight, `Enter` to select only when something's highlighted so exact-symbol-then-Enter still submits the form normally, `Escape` to close) plus click, and offers the same "not in our list, use it directly" escape hatch `StockSearch.tsx` already has, since this app never gates trading on the local assets table.
  - **Found while verifying the task's own literal example ("typing 'Intel' should surface INTC"), confirmed against real production data before touching anything: it didn't.** `searchAssets`'s ranking put every `"%intel%"` name-substring match into one undifferentiated tier, sorted alphabetically by symbol with no relevance weighting - and the real assets table has ~30 tradable "Artificial Intelligence"-branded funds, all of which sorted ahead of Intel Corporation itself purely because their symbols are alphabetically earlier, pushing INTC to position 20 of a 20-row limit. Fixed by adding a `name ILIKE 'query%'` tier between the existing symbol-prefix tier and the catch-all, in `lib/db/assets.ts` - a real fix to a shared function, not scoped to the ticket, so `/discover`'s own search benefits too. Re-verified against real production data after the fix: INTC now appears in the top 5 (previously 20th).
  - **Live quote display**: `StockQuoteCard` gained two new props, `compact` (smaller type/spacing for a 320px column instead of the full-width page-level display) and `showWideSpreadWarning` (defaults `true`, unchanged for every existing page-level use; `OrderTicket` passes `false` while placing a limit order, since that caveat is about the risk of transacting against the _current_ quote, which a limit order deliberately doesn't do) - one component, two call sites, not a near-duplicate. `OrderTicket` renders it immediately once a valid symbol is chosen, reusing the exact quote state (`getQuoteForSymbol`, debounced) it already had; a distinct "Fetching price…" state covers the gap between "symbol chosen" and "quote loaded" so a loading quote never briefly reads as "no live price," which `StockQuoteCard`'s own null-check would otherwise imply.
  - **Consistency check, as asked**: confirmed there was never a second ticket implementation to reconcile - `OrderTicket` was already the one shared component (dashboard: searchable; stock page: `fixedSymbol` locked), so both new features slot into that same existing prop rather than needing a refactor. Caught one real redundancy while building, not asked for explicitly: the stock detail page already shows this exact symbol's full `StockQuoteCard` above the tabs, always visible - `OrderTicket`'s own compact copy would put the same bid/ask/spread on screen twice at once there. Fixed by rendering the ticket's quote block only when `!fixedSymbol` - it earns its place only where it's the sole source of price info (the dashboard), which is a small extension of the exact same `fixedSymbol`-driven distinction the user asked to check for.
  - **Shown, not just described**: no browser tool is available in this environment, so a real screenshot isn't possible - built a static HTML preview instead, using the real hex values from `app/globals.css` and real search results pulled from the production assets table (the Intel/INTC ranking fix's own before/after data), covering six states (search dropdown open with a keyboard-highlighted row, market order with a live quote, market-closed dimmed quote, limit order with the quote-for-context-only behavior, no-live-quote banner, and the direct-lookup escape hatch) - delivered as a Claude Artifact, not just prose.
  - Unit tests: 283 (unchanged - `SymbolAutocomplete`'s interaction logic has no automated coverage yet, matching this app's standing gap around anything needing a rendered DOM; see Open questions). Integration tests: 54 (52 + 2 new, for the `searchAssets` ranking fix). Verified: `npm run verify`, a clean `next build`, and a route-level smoke test (dashboard/login render without a 500) against the local test database; the search-ranking fix specifically re-verified against real production data both before and after, not just the synthetic test fixtures.
- **Made Positions clickable, navigating to `/stock/[symbol]`** (`PositionRow`/`PositionCard`), matching `WatchlistRow`/`PopularStockCard`'s own existing pattern exactly: `PositionRow` (a `<tr>`, both the price-unavailable and normal branches) links only the symbol text, since a `<tr>` can't validly be wrapped in its own `<a>`; `PositionCard` (a `<div>`-shaped mobile card) wraps the whole card, since it can be.
- **Built the autonomous trading bot end to end**, following a two-phase design process explicitly asked for and followed: a researched, no-code proposal first (stop-loss recommendation, stock-selection feasibility given Alpaca's rate limits/Vercel duration, execution-model options, honest gaps), then a second proposal after the user redesigned the bot's own core principle away from prediction and toward "a transparent, pre-defined rule, measured honestly" - only after that second proposal was accepted did any code get written. See CLAUDE.md's new hard rule: **the bot never decides** - every buy/sell traces to one stated pure function, no LLM or "vibes" anywhere in the decision path.
  - **The rule** (`lib/trading/bot-rule.ts`, `rsi_pullback_uptrend_v1`): buy when RSI(14) < 30 AND the latest close is above SMA(50) (a pullback within an uptrend, not a falling knife); the rule's own exit signal is RSI recovering above 50. Deliberately one rule, not a blend - simplicity was an explicit, stated design goal, not a shortcut. Every parameter (`rsiPeriod`/`rsiEntryThreshold`/`rsiExitThreshold`/`smaPeriod`) is stored on the run that used it (`bot_runs.ruleParams`, jsonb), not hardcoded, so a future tuned version ships under a new rule id rather than silently reinterpreting past runs' results.
  - **Schema** (`lib/db/schema.ts`): a new `bot_runs` table (status machine: `selecting` → `holding` → one of four `closed_*` terminal states, or `failed_no_affordable_candidate`; rule id/params; capital; profit-target/stop-loss as a shared dollar-or-percent `bot_target_type` pair; `selectedSymbol`/`entryTotalCents`/`entryQuantity`/`realizedPnlCents`, all null until set) plus a nullable `orders.botRunId` tagging every order this app places on a run's behalf (entry buy, resting profit-target limit sell, eventual exit sell) - the measurement layer extends the existing ledger via a tag, not a parallel bot-trades table that could drift from it. Pushed to the local test database (`npm run db:push:test`); **hand-written migration ready but not yet applied to the real Supabase project** (`drizzle/0004_add_bot_runs.sql`), same "don't touch prod schema without asking" policy as every previous round, withheld pending explicit go-ahead.
  - **Position sizing** (`lib/trading/bot-sizing.ts`): capital fixed, quantity derived (`floor(capital / ask)`), never the reverse. Confirmed explicitly for the exact rounding-remainder case asked about: a $1,000 budget against a $310 stock buys 3 shares, spends $930, and `entryTotalCents` (not the nominal capital) is what P&L and target/stop-loss are measured against from then on.
  - **Targets** (`lib/trading/bot-targets.ts`): profit target and stop-loss share one `TargetConfig` shape (dollar or percent, basis points not a float), both mandatory, both validated against capital at config time before a run is even created (`validateTargetConfig`). `computeProfitTargetLimitPriceCents` rounds the resting limit sell's price UP, never down, so a fill always guarantees at least the stated target.
  - **Symbol selection** (`lib/trading/bot-selection.ts`): a curated watchlist (`BOT_WATCHLIST_SYMBOLS`, `lib/market/popular-symbols.ts` - reuses the existing 12-symbol Popular list; see the honest gap below), filtered by the already-built, already-empirically-confirmed `isSpreadImplausiblyWide` before the rule is even checked, then ranked by highest volume among whatever qualifies - a stated tiebreaker, not "most oversold," specifically because thin names produce more extreme RSI swings on light volume, which would otherwise bias selection toward exactly the illiquid names the spread filter is trying to screen out.
  - **Day-order expiry** (`lib/trading/bot-day-expiry.ts`): a holding run force-exits when the market is open but closing within 10 minutes (twice the worker's own assumed cadence) - strictly before the actual close, since `executeMarketOrder` rejects `market_closed` once it's shut, so waiting for the close itself would leave a position stuck open overnight.
  - **Worker orchestration** (`lib/db/bot-runs.ts`, invoked via `POST /api/worker/bot-runs` - a second, separate worker route mirroring the limit-order worker's own bearer-secret/`VERCEL_ENV` shape, not a shared route, since the two jobs need different data fetches and cadences): a selection cycle claims each `selecting` run via the same row-lock-then-recheck discipline as the limit-order worker's double-claim prevention, tries ranked candidates in order until one is both eligible and affordable, and - inside one transaction - fills the entry buy and places the resting profit-target limit sell together, reusing `applyFillEffects`/`recordFillTransaction` directly (no new money math) and `canPlaceLimitOrder` for the resting order's own reservation check. A monitoring cycle checks, per holding run: has the resting target already filled (bookkeeping-only close) → is stop-loss hit → is the market closing soon → has RSI recovered - a stated priority, not a judgment call, so simultaneous triggers resolve deterministically. `createBotRun` (`lib/db/bot-runs.ts`) validates and inserts a `selecting` row; `POST /api/bot/runs` (the Route Handler, not a Server Action - same bot-readiness architecture as limit orders) also runs one selection cycle immediately for faster feedback, swallowing a failure there since the run still exists and a later cycle will retry.
  - **The cancel-vs-fill race, tested explicitly as asked**: closing a holding run for any reason other than "the resting order already filled" must first cancel that resting order, and `cancelOrder`'s own row lock is what a concurrent limit-order-worker fill attempt would also be racing for - refactored `cancelOrder` into a thin userId-resolving wrapper around a new `cancelOrderByAccountId` (lib/db/orders.ts) so the bot (which has an accountId, never a userId) can call the exact same lock-and-check logic. Proven in both directions with real production code on both sides, not a reimplemented stand-in for either (`lib/db/bot-runs.test.ts`): one test engineers the limit-order worker's real fill to win the race (cancellation correctly reports `already_filled`, and the run would close as `closed_target`, not whatever it was about to close for); the other engineers the cancellation to win (the limit-order worker's own re-check after its lock correctly finds the order no longer pending and fills nothing). Both pass repeatedly, not just once.
  - **UI**: `BotRunForm` (capital, profit target, stop-loss, optional deadline - posts to the Route Handler) and `BotRunsPanel` (this account's own runs, symbol/status/entry/realized P&L) on the dashboard, plus `BotStatsPanel` - the aggregate win-rate/avg-win/avg-loss/sample-size view, grouped by rule id **and** params so a future parameter change never blends into an earlier version's sample (`lib/db/bot-stats.ts`, a single `FILTER`-clause aggregate query over closed runs only), visible from day one rather than a query run manually, per the explicit ask. Flags a small sample size visibly rather than letting an early win rate look more confident than it is.
  - **Deliberately not built, recorded here rather than half-done**: the GitHub Actions schedule for this new worker (same reasoning the limit-order worker's first round already established - a worker that can place real, if paper, trades unattended shouldn't run on autopilot before a manual test, and this round defaults to that caution even though it wasn't asked to be deferred explicitly this time); a way to cancel a `selecting`/`holding` run early from the UI; growing `BOT_WATCHLIST_SYMBOLS` past the 12 inherited from Popular - the design proposal's "a few hundred liquid names" was never built, since hand-curating and verifying a few hundred real tickers against the synced assets table is a real, separate data-curation task this round didn't do, not a design decision; the previously-discussed generic trade-journal/stats feature was deliberately NOT generalized - `bot_runs` is scoped to bot trades specifically, on the reasoning (given directly to the user) that a manual-trade journal has different, currently-unspecified needs and guessing at them now would just be design debt, with the extension point (an optional `bot_run_id` on a future generic table) left open rather than built.
  - Unit tests: 317 (283 + 34 new: `bot-rule`, `bot-selection`, `bot-day-expiry`, `bot-sizing`, `bot-targets`, `bot-pnl`). Integration tests: 73 (54 + 19 new across `bot-runs.test.ts` and `bot-stats.test.ts`, including both directions of the cancel-vs-fill race). Verified: `npm run verify` after every part as it was built (not batched at the end), plus a clean `next build` confirming `/api/bot/runs` and `/api/worker/bot-runs` both resolve as dynamic (ƒ) routes.

## Next

**The bot's schema is live and its worker route is smoke-tested (see Current status) - what's left is the one thing that needed real market hours, which the smoke test deliberately couldn't cover.** (1) ~~Apply `drizzle/0004_add_bot_runs.sql`~~ - **done**, verified directly against the real database. (2) ~~Manually hit `POST /api/worker/bot-runs` with the bearer secret~~ - **done**, against a throwaway account, market closed at the time. **Still outstanding**: repeat that same manual hit while the market is actually open (9:30am-4pm ET) - start a real bot run through the UI or the same throwaway-script technique, and watch a full live cycle: selection actually enters a real candidate (or correctly keeps waiting if none qualify at that moment), the resting profit-target order behaves like any other limit order, a real stop-loss or day-expiry exit actually places and fills a market sell. (3) Only wire up a GitHub Actions schedule for this worker after that live-market exercise, not before - same reasoning as the limit-order worker's own first round. (4) Once real trades start closing, watch `BotStatsPanel` - it exists specifically to test this round's own honest edge expectation (see Current status) against real numbers, not to be built and forgotten.

**Do a real, authenticated browser click-through of stock discovery AND both charts/visualization rounds first** - search for a symbol and by company name, browse Popular, star/unstar a watchlist entry from both `/discover` and `/stock/[symbol]`, buy and sell from the stock detail page, and on the chart specifically: confirm the volume histogram actually sits below the candles without overlapping, hover to confirm the crosshair readout updates with correct OHLC/volume, confirm the avg-cost dashed line appears only when a position is held and sits at the right price, confirm buy/sell markers land on the correct bar (not one day/bar off) at more than one timeframe, switch between every Interval/Range combination and confirm the invalid ones are actually disabled rather than just slow, toggle SMA/EMA/RSI on and off and confirm the RSI subpanel doesn't visually collide with volume, toggle log scale on a 1Y view, and click through all three tabs (Chart/Position/Orders) confirming price/spread stay visible throughout. Also check the dashboard's allocation bar and Positions/watchlist sparklines render sensibly at a few different position counts, and try the "not in our list, look up directly" escape hatch with a real ticker not yet synced. All three rounds so far were verified at the route level (no 500s, auth redirects correct, clean production build, full unit/integration coverage) - nothing has actually been seen rendered in a browser yet, and real comparison/API-assumption bugs were caught by re-reading diffs and re-verifying API behavior directly rather than by any test in both chart rounds - a visual check is what would catch the next one of that flavor if it exists.

**Then re-attempt the Vercel deploy, then run the full click-through against the deployed URL** (see the checklist delivered in conversation - includes placing a real order during market hours, and firing concurrent requests at `/api/check-db` to actually stress the pooler rather than just confirming it responds once). Env vars, scopes, and the preview-deployment decision were walked through in conversation and belong in Vercel's dashboard, not in this repo. `npm run sync-assets` should be re-run before that deploy, or scheduled somewhere (a cron job, a Vercel scheduled function) - right now it's still a manually-triggered script with no automatic re-run.

**Limit orders need a real click-through and a real open-market exercise, neither done yet.** Everything this round was built and integration-tested against the local test database with `fetchQuotes`/`isMarketOpen` mocked - the real market was closed the entire time this was built, so the worker's fill-checking branch (as opposed to its expire-sweep branch) has never actually run against a live Alpaca quote outside a test. Before trusting this in production: (1) a real browser click-through - place a limit buy, watch it sit pending, manually hit the worker route once the real market is open with a limit price you know will cross, confirm it fills and the position/cash update; place one that won't cross, confirm it stays pending; cancel one; let one expire at a real close. (2) ~~Push the schema to the real Supabase project~~ - **done**, see Current status (forced by a real production incident, not scheduled work). (3) Set `LIMIT_ORDER_WORKER_SECRET` on the real deployment and the `WORKER_URL`/`LIMIT_ORDER_WORKER_SECRET` GitHub repo secrets, per the README's new Setup step 7 - still not done, and the app is live, so this is the actual next step now, not a someday one. (4) Watch `GET /api/worker/status` for the first real day of scheduled runs to confirm the GitHub Actions schedule is actually firing (best-effort, not guaranteed) and the staleness thresholds read sensibly against real timing, not just the values chosen from reasoning alone. (5) New, found only because the app is now actually live: check whether anything else in `main`/the deployed branch's history depends on schema this session never pushed - this round's incident was caught because the dashboard crashed loudly; a quieter schema gap elsewhere might not announce itself the same way.

## Open questions / deferred

- **An equity curve (portfolio value over time) is not possible with what this app currently records, and needs a real decision about how to start.** The transactions ledger (`lib/db/schema.ts`'s `transactions` table) records every cash delta with a running `balanceAfterCents`, but never a snapshot of total equity (cash + market value) at a point in time - market value depends on a live quote, which was never persisted. Two real options, not just one:
  - **(a) Forward-only daily snapshots.** Add an `equity_snapshots` table (accountId, date, cashCents, totalMarketValueCents, totalEquityCents) and a job that writes one row per account per day. Needs a trigger: either a scheduled job (Vercel Cron hitting a new route, once this app is deployed and has cron available) that snapshots every account at a fixed time such as market close, or an opportunistic snapshot taken whenever a user visits the dashboard (no cron needed, but misses days nobody visits, leaving gaps). The chart itself is straightforward once the data exists - a line/area series, reusing the same lightweight-charts pattern already built for `StockChart`. The real cost is that **history only starts accumulating from the day this ships** - weeks or months of past activity are simply gone from the curve.
  - **(b) A one-time retroactive backfill.** Since every fill is already in `orders`/`transactions` with an exact timestamp and price, it's possible to reconstruct what was held on any past day and value it using Alpaca's historical daily closes (the same bars endpoint `fetchDailyBars`/`fetchDailyBarsForSymbols` already use) - replay the order history day by day, track cash and positions as of each day's close, mark to that day's close price. This gives immediate full history back to account creation instead of an empty chart that only fills in going forward. The tradeoffs: meaningfully more upfront engineering than (a) alone (a batch reconstruction job, careful handling of non-trading days and any symbol that's since been delisted, and every historical point is an EOD close, not a true intraday mark - the curve can't show what equity was mid-day, only at each day's close). It also doesn't replace (a): a backfill only produces history up to "now," so forward-only recording is still needed afterward to keep the curve current.
  - **Recommendation if this gets built**: do both together, not (a) alone. Given this app already has everything (b) needs (a complete, exact-timestamped order ledger and access to historical closes), starting the curve at an empty chart when a populated one is achievable in the same effort seems like the wrong default - the backfill is a bounded, one-time cost, not an ongoing one. Worth scoping as its own session rather than folding into a future chart tweak.
- **The asset sync is idempotent and self-logging but still entirely manual - nothing runs it automatically.** `getLastSuccessfulAssetSync()` + `AssetSyncNotice` mean staleness is at least visible on `/discover`, but the underlying gap (new listings/delistings only get picked up when someone remembers to run `npm run sync-assets`) is unresolved. Worth a Vercel Cron job or similar once this is deployed - not done here since it depends on the deploy target's own scheduling mechanism, which the pre-deploy sessions in this log haven't set up yet.
- **2028 Christmas Eve early close is genuinely unresolved**, not just unverified. NYSE hasn't published that far ahead. `lib/trading/market-hours.ts` deliberately omits a `2028-12-24` (or `2028-12-22`) entry rather than guess — check `nyse.com/markets/hours-calendars` closer to the date. `HOLIDAY_DATA_VALID_THROUGH` + a test failing 90 days out will surface this automatically.
- **Supabase pooler behavior under real concurrent serverless load is still unverified as of this session** — `max: 1` + the `/api/check-db` route are this session's answer to it, but neither means anything until they're actually exercised against a deployed instance under concurrent requests. This is the specific thing the next session's click-through needs to close out, not assume.
- **A held position's "current price" is the bid, not a live tick.** `calculatePortfolio` marks positions to the bid (what a sell would actually fill at right now), and the dashboard's `isStale` flag is derived only from whether the market is open, not from checking each quote's own timestamp against the 60-second staleness window `lib/trading/execute.ts` uses for order rejection. In practice these agree, since quotes are fetched fresh on every request with no cache — but if a quote cache is ever added in front of Alpaca, this needs revisiting so a truly stale individual quote doesn't render as live.
- **The order ticket's estimated cost can go stale between the debounced quote fetch and actual submission** (e.g. the user pauses before clicking Buy). This is disclosed in the UI ("Estimate only... can differ") and `stale_quote`/price-drift rejections are handled, but there's no re-fetch-on-submit — worth revisiting if rejections on this reason turn out to be common in practice.
- **RESOLVED: whether `isSpreadImplausiblyWide`'s 300bps threshold generalizes past the closing-bell case, or only ever fires as a "market is closed" detector.** Was left open with a specific resolution path ("confirm 3% threshold behavior against a real thin small-cap during market hours"); that check has now been done. Pulled real historical intraday quotes for a regular session, 10:30-11:30am ET - nowhere near the open or close - for names off Alpaca's own most-actives list: CELZ (a real penny stock) averaged an 8.08% spread that session (one tick hit 37.8%), RCON averaged 18.95% (one tick hit 62%), VIVS averaged 30.43% (one tick hit 169.5%) - all real, continuously-quoted, genuinely thin markets, not artifacts of anything. Two more liquid names from the same list and the same window, SNXX and SPCH, stayed under 0.5% average the entire session. Confirmed: the threshold correctly fires on real intraday illiquidity and correctly stays quiet on real liquid markets, regardless of time of day - it generalizes, and there's no remaining ambiguity to leave open. It was also already unconditional on market hours in the code (never gated by `isMarketOpen`/`isStale`), so there was nothing to decouple - that part of the original design was already right. `describeWideSpreadWarning`'s wording was updated to name both confirmed causes (the stock's own real illiquidity, or this app's single-exchange feed) rather than over-attributing every case to IEX-at-the-close. All six real closing-print quotes (NVDA/TSLA/AAPL/MSFT/AMZN/SPY) and two of the real intraday ticks (CELZ, SNXX) are now fixtures in `lib/trading/quote.test.ts` and `lib/market/alpaca.test.ts`, not just described here.
- **Still open, and now with sharper evidence behind it: should `placeMarketOrder` also gate on an implausible spread, not just warn on the display side?** The intraday check above used real, actively-quoted symbols during real historical market hours - VIVS's worst tick that session was a 169.5% spread on a stock that was genuinely trading, not halted. A real order placed against a quote like that, if the market happened to be open (the `market_closed` rejection only covers the closed-market case), could fill catastrophically far from any reasonable price, with no rejection at all today. Deliberately left unfixed rather than silently blocking trades on a numeric heuristic, since that's a bigger behavior change than a display warning and could reject a legitimately volatile stock's real, if wide, market - a real product decision, not a bug fix, and one this session hasn't been asked to make.
- **Found, not fixed, while pulling real fixtures above: this app's cents-as-bigint money model can't represent the sub-penny quotes real penny stocks actually trade at.** RCON's real mid-session quote was bid $0.0316 / ask $0.06 - `toCents` only accepts up to 2 decimal places, and `fetchQuote`/`fetchQuotes` already call `.toFixed(2)` on every raw Alpaca float unconditionally, so a quote like that one is silently rounded (0.0316 -> "0.03") before it ever reaches `Cents`. For a $0.03 stock, that's a real, double-digit-percent rounding error, not a rounding-to-the-nearest-cent nicety - it would be for a normally-priced stock. Not fixed here (this session was asked to resolve the spread-threshold question, not redesign the money model for sub-cent precision), but worth a real look if this app is ever meant to support genuinely low-priced/penny stocks rather than the large/mid-cap names it's been tested against so far.
- **Two adjacent (currently harmless) risks found while auditing the suite for the `DB_POOL_MAX` class of problem, not acted on since nothing is broken today:** (1) `vitest.config.ts`'s `passWithNoTests: true` means the unit run exits 0 if its glob matches zero files - a typo'd `.test.ts` extension or an over-broad future `exclude` would silently drop coverage to zero without failing anything. (2) `vitest.integration.config.ts`'s `fileParallelism: false` exists specifically because integration tests share one database and a concurrent file's `truncate` would corrupt another's fixtures - a future "speed up CI" edit could remove it without knowing why it's there, reintroducing that corruption. Neither is a live bug; both are exactly the shape of thing worth a second look if integration tests ever start flaking.
- **Placing a limit order while the market is closed is accepted, not rejected - and can be expired within minutes of being placed, not preserved until the next session.** The worker's day-order sweep (`expireAllPendingOrders`, `lib/db/limit-order-worker.ts`) expires every pending limit order on any closed-market invocation, unconditionally - a deliberate simplification chosen specifically because it stays correct under an irregular/gapped schedule with zero extra bookkeeping (no need to know when an order was placed relative to when the worker last ran). The real cost: an order placed at 5pm for "tomorrow's session" will very likely be swept away by the next scheduled tick (every 5 minutes), not preserved. Not treated as a bug - genuinely a different, simpler definition of "day order" than a real broker's - but worth flagging to a user who expects after-hours limit orders to survive to the next open, and worth reconsidering if that turns out to matter in practice.
- **Whether `placeLimitOrder` should also reject placement outright while the market is closed, given the above, was considered and deliberately left alone.** Blocking placement would prevent the confusing "accepted, then expired minutes later" experience, but the user's design brief didn't ask for it and it's a real behavior/UX decision, not a bug fix - noted here rather than decided silently.

## Gotchas discovered

- **The concurrency test needed a synchronized mock delay to be trustworthy.** A plain `Promise.all` of two concurrent order placements looked like it tested the row lock, but timing was unreliable: one call's async setup could fully finish (and commit) before the other even started, so the test sometimes passed even with the lock deliberately removed. Fixed by delaying the mocked quote fetch by a fixed amount so both calls clear their pre-transaction work in near-lockstep, forcing genuine overlap — then verified by removing the lock and confirming the test _actually_ failed 4/4 runs, and passed 4/4 with the lock restored.
- **`npm run verify` and `npm ci` catch different classes of failure.** `verify` never reinstalls dependencies, so a `package.json`/`package-lock.json` mismatch is invisible to it — it only surfaces in CI's `npm ci`. That's why the pre-push hook now also runs `npm ci --dry-run`.
- **A skill directory named `design` silently collided with a built-in CLI command** (`/design`). The Skill tool refused to invoke it with an explicit error rather than doing something confusing. Renamed the directory to `trading-ui-design`.
- **A color token named `base` collided with Tailwind's built-in `text-base` utility** (font-size: 1rem) — defining `--color-base` made `text-base` ambiguous between font-size and text-color. Added a separate `--color-on-fg` token for the one place text needed to match the background color, instead of overloading `--color-base`.
- **`.claude/agents/` and `.claude/skills/` need a full quit-and-reopen to be picked up if the directory didn't exist when the session started** — not just a new message, and not fixable by telling Claude to "restart." Both the `reviewer` agent and the `trading-ui-design` skill hit this; work-arounds (running the agent's instructions through a general-purpose agent) confirmed the content was correct while the registration lag was purely a session/watcher issue.
- **Joining two independent one-to-many children onto the same parent in one query fans out into their cross product.** `getPortfolio`'s single-round-trip query LEFT JOINs `positions` and a 20-row `orders` subquery onto `accounts` in the same SELECT — for an account with P positions and O orders, that's P×O rows back, not P+O. Harmless at this app's scale (a handful of positions, orders capped at 20), but it must be deduplicated in JS (by symbol, by order id) rather than trusted as one-row-per-position — the integration test with 2 positions and 2 orders is what would catch a regression here.
- **Next.js 16 deprecated the `middleware.ts` file convention in favor of `proxy.ts`.** `next build` prints an explicit warning with a codemod command (`npx @next/codemod@canary middleware-to-proxy .`) — ran it rather than hand-renaming, since it also renames the exported function (`middleware` → `proxy`), not just the file.
- **Signup and login were verified structurally, not end to end against the live Supabase project.** Confirmed via build + curl: `/dashboard` redirects to `/login` when signed out (307), `/login` and `/signup` render with the expected form fields, typecheck/tests/format/build all pass. NOT run: an actual `signUp`/`signInWithPassword` round trip against the real project — doing that from here would create a real, permanent row in that project's `auth.users` with no service-role key available to remove it afterward, which is exactly the kind of action on shared external state this project's own conventions say to check before taking. The user's manual click-through is what actually proves the live path works. **Update: the user has since completed this click-through and confirmed it all works** (see Current status) — this entry is kept as a record of how it was verified in two stages, not as an open item.
- **A component file that imports a Server Action can become untestable in the default unit suite**, even for logic that's otherwise pure. `OrderTicket.tsx` imports `placeTrade`, which imports `lib/db/client.ts`, which throws immediately if `DATABASE_URL` isn't set — so importing anything at all from `OrderTicket.tsx`, even just a plain string-building helper like `describeRejection`, crashed the test file before a single test ran. Fixed by moving `describeRejection` into `components/orderMessages.ts`, which imports only `lib/trading/*` and `MarketStatusBanner` (itself DB-free). The same pattern already existed for `MarketStatusBanner`'s `openMessage`/`closedMessage` and `Delta`'s `formatDeltaAmount` - export the pure text-building function so it stays testable without a DOM, but watch what THAT file transitively imports, too.
- **Changing `formatCents`'s output format (adding thousands separators) could have silently broken its own round-trip test** (`toCents(formatCents(x)) === x`), which exists specifically to guarantee no precision is lost between the two functions. Extending `toCents`'s grammar to also accept grouped input (not just relaxing the test) is what kept the existing test meaningful and passing unchanged — a reminder that when a function's contract changes, the fix is usually to update its counterpart's contract too, not to loosen the assertion checking them against each other.
- **The `server-only` package throws unconditionally under plain Vitest**, breaking every test file that transitively imports a guarded module - not a false positive, just a real gap between how Vitest resolves modules and how Next's webpack build does. Next's build applies a conditional swap (real guard for the client, no-op for the server) that Vitest has no equivalent for, so it always gets the real, always-throwing version. Fixed by aliasing `"server-only"` to a local no-op shim (`vitest.server-only-shim.ts`) in both Vitest configs - the same fix `next/jest` ships for exactly this reason, so this wasn't a one-off workaround.
- **`pkill -f "next start"` (and similar) does not reliably kill Windows `node.exe` processes started via an npm script**, in this Git Bash environment. A stale server from an earlier `npm run start` survived multiple later `pkill` calls across this session and kept silently answering `curl` requests on port 3000 - which looked exactly like a real bug (stale page title, `/` not redirecting) until `netstat -ano | grep :3000` showed the same PID had been LISTENING the whole time. Fixed by killing the exact PID with `taskkill //F //PID <pid> //T`. When a local verification result looks inexplicable right after a code change, check `netstat`/`tasklist` for a leftover listener before assuming the code is wrong.
- **Capping `lib/db/client.ts`'s connection pool to `max: 1` (for the serverless-correctness reasons above) silently broke the row-lock concurrency test's actual validity, without making it fail.** The test still passed 10/10 after that change - but only because `max: 1` means the two concurrent `placeMarketOrder` calls it runs could never actually have two connections open at once, so postgres.js serialized them client-side before either transaction reached Postgres. The _outcome_ the test asserts (exactly one order succeeds) is trivially true under that serialization too, so nothing failed - but the test was no longer exercising the `SELECT ... FOR UPDATE` lock it claims to prove.
  - **Why it was invisible**: nothing about this shows up in a test report. `npm run test:integration` printed the same "10 passed" whether the row lock existed or not - the failure mode is a test that quietly stopped testing anything, not a test that errors. The only way to see it was to deliberately break the thing the test claims to protect (comment out `.for("update")`) and check whether the test noticed. It didn't, the first time.
  - **How it was caught**: during a pre-push review, by re-running exactly the empirical check the test's own comment already describes doing once, historically ("verified by removing the lock and confirming the test _actually_ failed 4/4 runs"). That check had never been redone after `max: 1` was introduced.
  - **The fix, in two layers**: (1) a `DB_POOL_MAX` env var - unset (so `max: 1`) in production, set to `5` only in `vitest.integration.setup.ts` - restores genuine two-connection concurrency for the test process without changing production behavior. (2) `lib/db/client.ts` exports `poolMax`, and the concurrency test itself now asserts `poolMax >= 2` _before_ running, with a message explaining exactly why a lower value would make it pass trivially - so if this regresses again (`DB_POOL_MAX` unset, lowered, or renamed), the test fails loudly with an explanation instead of passing silently. Verified in both directions again after adding this: temporarily set `DB_POOL_MAX=1` in the setup file → the precondition itself fails with the intended message; restored to `5` → passes.
  - **Also added**: `.claude/agents/reviewer.md` check 8, "config changes that could invalidate an existing test without failing it" - triggered by diffs touching connection pooling, concurrency limits, timeouts, caching, or environment-conditional behavior, instructing the reviewer to identify affected tests and re-run the same break-it-and-check verification, not just reason about whether it's probably fine.
  - **The general lesson**: a config change made for one good reason (serverless correctness) can silently invalidate a test written under different assumptions (needing real concurrency) without that test ever going red - "the suite is green" and "the suite is still testing what it says it's testing" are not the same claim, and a change to shared infrastructure code is exactly when to re-check the second one, not just the first. A precondition assertion is the durable fix; a one-time manual re-verification is not, since nothing forces it to happen again next time.
- **Vercel only accepts even-numbered Node LTS majors (20, 22, 24, ...) - odd majors (21, 23, 25, ...) are always "Current," never LTS, and Vercel rejects them as a deploy target outright.** `engines.node` had been set to `25.6.1` because that happened to be the version installed on the dev machine at the time, not because anyone checked it against what the deploy target actually accepts - the mismatch only surfaced when a real deploy failed. `engines.node` (and `.nvmrc`, which must match it) must be chosen by checking Node's own release schedule / Vercel's supported-versions doc, never by running `node --version` locally and copying the answer. `scripts/check-node-engine.ts` (wired into `npm run verify`) now catches this locally before a push: it fails loudly if `engines.node`'s major version is odd, with no version list to keep updated, since "even is LTS, odd never is" is Node's permanent policy, not a snapshot.
- **A script's own top-level env-file loading can run too late relative to its own imports, because ES module imports are hoisted above the rest of the file's code.** `scripts/sync-assets.ts` called `process.loadEnvFile(".env.local")` in its own body, but a static `import { syncAssets } from "../lib/db/assets"` above it is evaluated first regardless of source order - and that import chain reaches `lib/db/client.ts`'s module-level `if (!process.env.DATABASE_URL) throw ...` before the env file had loaded, so the script crashed with "Missing DATABASE_URL" every time despite the loading code being right there in the file. Fixed by deferring the import: `process.loadEnvFile` first, then `const { syncAssets } = await import("../lib/db/assets")` inside an async `main()` - a dynamic import is not hoisted, so it only runs once actually reached. Watch for this in any future script that both loads `.env.local` itself and imports something that reads `process.env` at module scope (not inside a function body) - `lib/market/alpaca.ts`'s functions read env vars lazily inside each function for exactly this reason, but `lib/db/client.ts` reads `DATABASE_URL` eagerly at the top of the module.
- **`core.autocrlf=true` (Git for Windows' default) plus no `endOfLine` setting in `.prettierrc` is a standing trap on this machine, unrelated to any specific change.** Checked-out files get CRLF on disk; Prettier defaults to expecting LF; `format:check` can start failing across the whole repo for files nobody touched, and it isn't tied to any particular git operation that was ever fully identified (a `.gitattributes` `eol=lf` rule was added but didn't reliably force LF back on this checkout path). Fixed durably with `"endOfLine": "auto"` in `.prettierrc`, which makes Prettier preserve whatever line ending is already in a file instead of asserting one - if `format:check` ever fails broadly again with no corresponding content change, check this before assuming something is actually broken.
- **A `lightweight-charts` v5 series option can look like it belongs on the price scale and fail at compile time, not at review time, if it doesn't.** `autoscaleInfoProvider` (pinning RSI's subpanel to a fixed 0-100 range) configures the price scale's own behavior but is actually passed to `addSeries`, not `chart.priceScale(id).applyOptions()` - TypeScript's structural typing caught the misplacement immediately (`DeepPartial<PriceScaleOptions>` has no such property), which is the good case; the risk is options that happen to type-check in the wrong place. When adding a new chart option, verify which object the library's own typings declare it on before assuming based on what it conceptually configures.
- **Alpaca's bars endpoint treats both `start` and `end` as inclusive bounds** - confirmed empirically (querying `end=T` and, separately, `start=T` both returned the bar timestamped exactly `T`), not assumed from docs. This is what let the completed-range and forming-range queries in `fetchBars` both return the exact bar sitting on their shared boundary instant, duplicating it on the chart - fixed by subtracting 1ms from the completed query's `end`. Worth remembering for any other Alpaca endpoint with a `start`/`end` pair: don't assume the usual half-open convention without checking.
- **A "hidden overlay price scale" (a second named price scale confined by `scaleMargins` + `visible: false`) confines a series' bars but not its `lastValueVisible` last-value badge.** This is what let volume's "606.89K" label bleed into the price panel's own coordinate space even though the volume bars themselves stayed correctly confined to their band - a real, confirmed limitation of that technique, not a `scaleMargins` tuning problem. lightweight-charts v5's real multi-pane API (`addSeries(type, options, paneIndex)`) doesn't have this problem, since the label lives in a genuinely separate pane rather than a differently-scaled region of the same one.
- **A quote's bid/ask being present and being valid are two different checks, and this codebase only ever made the first one.** Alpaca reports `0` for a side with no active quotation (confirmed the real cause of a real screenshot: Bid $295.94 / Ask $0.00 / Spread −$295.94 rendered while the market was closed) rather than omitting the field or erroring - so `bp: 0`/`ap: 0` parses as a perfectly valid `z.number()` and sails straight through Zod's boundary validation. Every render site in this app already had a "no quote for this symbol" branch (`bidCents === null || askCents === null`), but that branch only fires for a symbol Alpaca omits entirely - a zero-priced side isn't null, so it skipped every one of those checks and reached the display and the spread math as if it were a real price. Fixed once, at the actual boundary (`fetchQuote`/`fetchQuotes` in `lib/market/alpaca.ts`, backed by `isValidTwoSidedQuote` in `lib/trading/quote.ts`), rather than patching each render site separately - a zero-priced side is now treated identically to a missing quote before it ever leaves that file. Watch for this in any future Alpaca (or other external) integration: a schema type check (`z.number()`) proves a field parses, not that its value is meaningful - "present" and "valid" need a separate check whenever an API can legitimately return a sentinel value (0, empty string, etc.) for "no data," and a boundary that only checks presence will let that sentinel through as if it were real.
- **The same missing-quote gap was more than a display bug - it reached the actual order-fill path.** `placeMarketOrder` (`lib/db/orders.ts`) prices a real fill from the same `fetchQuote` call, and before this was fixed, a zero-priced ask/bid could have filled a buy/sell at $0.00 the moment it occurred, not just rendered oddly - and the existing `market_closed` rejection in `executeMarketOrder` doesn't cover this case, since Alpaca can report a zero-priced side during market hours too (thin liquidity), not only while closed. Found by deliberately checking "does this same bug reach anywhere money actually moves," not just the places it was originally reported rendering - the display bug and the execution-path risk turned out to share one root cause and one fix, but only because the execution path was checked at all.
- **Rewriting `lib/market/alpaca.test.ts` from scratch (via `Write`, without reading what was already there) silently deleted a real, unrelated `fetchQuotes` test suite** that had nothing to do with the change being made. Caught by reviewing `git status`/`git diff` before committing, not by any test failure - the suite still passed with the file replaced, since nothing else exercised those deleted cases. Restored the original tests and merged the new ones in alongside them. The `Write` tool requires reading a file first specifically to make this mistake harder, but reading doesn't prevent overwriting; it's what you do with the read content when composing the replacement that either preserves or destroys it. Prefer `Edit` (append/insert) over `Write` (full-file replace) for extending an existing test file with new, unrelated cases.
- **A plain `fetch()` to a Route Handler does not get the automatic client Router Cache invalidation a Server Action gets for free.** `placeTrade` (a Server Action) calling `revalidatePath` inside it is enough on its own to make the calling client re-fetch fresh data - that's part of the Server Actions protocol. `placeLimitOrder`/`cancelOrder`'s Route Handlers also call `revalidatePath`, but since `OrderTicket`/`PendingOrdersPanel` reach them via a plain client-side `fetch`, that only marks the server-side cache stale - nothing tells the browser to refetch. Missed on the first pass (copied the "no `router.refresh()` needed" reasoning from the existing Server-Action-based flow without re-checking whether it still applied to a different call mechanism), caught by reasoning through what actually wires a Route Handler's `revalidatePath` back to the calling client, not by a test. Fixed by calling `router.refresh()` explicitly after every successful limit-order placement/cancellation. Worth remembering for any future Route-Handler-first feature in this app: `revalidatePath` inside the route is necessary but not sufficient when the caller is a plain `fetch`, unlike when the caller is a Server Action.
- **Two operations that each lock a row must always lock in the same order, or two concurrent operations locking the "same two things" in opposite orders can deadlock.** `placeLimitOrder` locks only the account row; `cancelOrder` locks only the order row; the worker's fill path locks the order row first, then (only if it's actually going to fill) the account row - never the reverse, and never both at once from any other path. Documented here as an invariant to preserve, not just a fact about the current code: a future change that makes any operation lock an account, then reach for a specific order's lock (or vice versa, in a different order than the worker already established) would reintroduce a real deadlock risk between the worker and whatever that new path is, not something a quick test run would necessarily catch under normal load.
- **A "don't touch the real production schema without asking" policy protects against the wrong half of the risk if code that depends on the new schema still gets deployed anyway.** This exact thing happened: the limit-orders round's code went out (pushed, and the app auto-deployed - apparently already live at `trading-sim-sigma.vercel.app`, which this log hadn't even been tracking) while the schema push was deliberately withheld pending the user's go-ahead, and the result was a real 500 on the live `/dashboard` - `getLastLimitOrderWorkerRun()` querying a table that only existed locally. Being cautious about the database half of a change while shipping the code half anyway doesn't avoid the risk, it just moves where it surfaces - a real user hitting a real error, instead of a schema push landing when it wasn't wanted. Worth asking about BOTH halves together next time a feature needs new schema, framed as one decision ("this deploy needs a matching schema push, in the same conversation, or the code shouldn't go out yet") rather than treating "hold off on the database" as the safe default in isolation.
- **The real Supabase database still cannot take a plain `drizzle-kit push`, confirmed again on a second, unrelated schema change** (adding `limit_order_worker_runs` + the `order_status` enum's `"expired"` value) - the exact same crash (`checkValue.replace` on `undefined`) as the original `drizzle-team/drizzle-orm#4496` finding in `drizzle/0001_add_assets_and_sync_runs.sql`. This is a property of the real database's own constraint representation (something Supabase-specific confuses drizzle-kit's introspection), not of any particular schema change - every future real-database migration should expect to need `scripts/apply-migration.ts` and a hand-written SQL file, not attempt `db:push` first and only fall back once it fails. `ALTER TYPE ... ADD VALUE` for a new enum value and unrelated `CREATE TYPE`/`CREATE TABLE` statements combine fine in one transaction via that script, as long as the new enum value is never itself referenced later in that same transaction (Postgres forbids using a value added earlier in an uncommitted transaction) - this migration's own comment documents why it's safe here.
- **A floating dropdown with no explicit height cap will grow to fit however many results come back, and in a dense panel layout that means overlapping whatever's below it, not just looking tall.** `SymbolAutocomplete`'s first version had no `max-height`/`overflow-y` on its results list - fine for a handful of matches, but a real query ("INTEL," 20 results before the `searchAssets` ranking fix even mattered) grew tall enough to cover the dashboard's Positions table and the pending-orders staleness banner underneath it, confirmed from a real screenshot, not caught by anything automated (this app still has no rendered-DOM test setup to catch a layout overlap either way). Fixed with a capped `max-h-72 overflow-y-auto`, plus an explicit `scrollIntoView` on the highlighted row when it changes via keyboard - without that second part, capping the height alone would have silently broken keyboard navigation past the visible rows (the highlight would move somewhere the user can't see). Any future floating/absolutely-positioned list in this app (another autocomplete, a context menu) should assume it needs both from the start, not add them reactively after someone notices the overlap.
- **Calling an existing DB-layer function as a black box inside a new transaction is a real nesting risk, not just a style preference, once that function opens its own transaction internally.** The bot's entry flow needed to place both a market buy and a resting limit sell atomically alongside a bot-run row lock; `placeMarketOrder`/`placeLimitOrder` each already open and manage their own `db.transaction`, so calling either from inside `tryEnterBotRun`'s own transaction would have held two pooled connections open for one logical operation, not one - fine at this app's tiny scale, but exactly the kind of thing that stops working the moment the connection pool is small (see `DB_POOL_MAX` in `lib/db/client.ts`). Fixed by reusing the shared _sub_-helpers those functions themselves are built from (`applyFillEffects`/`recordFillTransaction`, and a new accountId-scoped `cancelOrderByAccountId` extracted from `cancelOrder`) rather than the top-level functions - the money math still isn't duplicated, but the transaction boundary stays singular. Worth checking for this specifically before calling any existing `lib/db/*.ts` function from inside a new transaction in this codebase: does it open its own `db.transaction`, and if so, is there a lower-level shared piece to call instead.
- **Mocking a module wholesale for a test (`vi.mock(path, () => ({...}))`) replaces every export, including ones the test file never calls directly but that the code under test's own transitive imports still need at module-evaluation time.** `lib/db/bot-runs.test.ts` mocked `lib/trading/indicators` to control `rsi`/`sma` without feeding a real price series - but `lib/trading/bot-rule.ts` also imports that module's `DEFAULT_RSI_PERIOD` as a top-level constant (`RSI_PULLBACK_UPTREND_V1_PARAMS`'s own initializer), which isn't a function call the mock could lazily supply - it's read once, immediately, when the module loads. Left out of the mock's returned object, this crashed with "No DEFAULT_RSI_PERIOD export is defined" before a single test ran. Worth checking what ELSE a module exports - and whether anything in the dependency chain reads it at module-load time rather than call time - before mocking it wholesale, not just what the test itself calls.
